# VIDEO ZERO storyboard v2 generation notes

Regenerated 2026-07-11 from `REDLINE ROUND 1` in `docs/course-production/shotlists/videozero-cold-open-scenecards.md`.

Generation used the built-in image tool with the standard design references. Relevant shots also used the Andrea standing anchor, `andrea_car_v2.png`, the generated continuity frames, and the module-1A BMH building canon. All outputs are 1600 x 900 PNG files.

## Frame notes

- `mV0_LV0_s01_house-wide.png`: Andrea's modest home sits within a full neighborhood row. The street, sidewalks, driveways, green lawns, layered green trees, neighboring houses, and clouds replace the isolated v1 composition.
- `mV0_LV0_s02_keys-detail.png`: Andrea's pure-white hand and yellow cuff hold two detailed keys against her orange door, cream trim, and green porch foliage.
- `mV0_LV0_s03_home-door.png`: The closed orange home door is centered for Match Cut A. The full porch, steps, planters, sidewalk, lawn, neighboring edges, trees, and clouds remain visible.
- `mV0_LV0_s04_walk-to-car.png`: Headset-free Andrea walks through a populated neighborhood toward the continuity orange four-door sedan. Her face and hands were normalized to pure white.
- `mV0_LV0_s05_sidescroll.png`: The continuity sedan appears in true profile with foreground mailbox and foliage, lane-lined street, and a detailed row of homes, driveways, sidewalks, lawns, trees, and clouds.
- `mV0_LV0_s06_windshield-pov.png`: Hands-on-wheel driver POV reaches the `andrea_car_v2.png` richness bar with mirrors, pillars, gauges, vents, radio controls, dashboard detail, and a populated neighborhood through the glass. All gauge text was removed and both hands were normalized to pure white.
- `mV0_LV0_s07_topdown-grid.png`: Full overhead rework with detailed roof ridgelines, gables, chimneys, driveways, yards, sidewalks, intersections, lane lines, green trees, and one orange car. No blank rooftop boxes remain.
- `mV0_LV0_s08_notice-door.png`: The whole distressed house is visible in neighborhood context. The yard is overgrown green, the walkway is cracked, and `NOTICE` is the only text.
- `mV0_LV0_s09_rearview.png`: The distressed house recedes inside the orange-trimmed rearview mirror while a detailed neighborhood road continues ahead. The distant door paper and gauges contain no glyphs.
- `mV0_LV0_s10_bmh-lowangle.png`: Low-angle arrival uses the module-1A yellow building, orange accents, blue entrance, green `B|M|H` sign, green landscaping, detailed parking stalls, parked cars, and the continuity orange sedan.
- `mV0_LV0_s11_follow-entrance.png`: Headset-free Andrea is followed from behind across the parking lot toward the same canon building. The orange sedan remains parked behind her. Visible skin was normalized to pure white.
- `mV0_LV0_s12_bmh-door.png`: Centered Match Cut B preserves the canon yellow facade, orange columns, blue double doors, green `B|M|H` sign, green planters, paving, trees, and clouds.
- `mV0_LV0_s13_the-turn.png`: Andrea faces the lens with calm resolve in front of the canon entrance. Her face and hands were pixel-checked as pure white. She has no headset.

## QC record

- Archived all 13 v1 frames unchanged in `_v1-sparse/` before replacement.
- Full-resolution review completed for all 13 v2 frames.
- Every final file is 1600 x 900 RGB PNG.
- Connected blue backgrounds were normalized to canonical `#62b3f3`.
- Green-family foliage is present everywhere nature appears.
- The orange four-door sedan remains visually consistent across all car shots.
- Skin correction gate passed with zero misses and zero clothing bleed on corrected frames. The final turn frame already used pure-white face and hand pixels and did not require correction.
- Baked text review passed. `NOTICE` is exact in s08. The canon `B|M|H` sign is exact in s10 through s13. No other text or pseudo-text remains.
- No blank placeholder shapes remain. The neighborhood, overhead grid, car interiors, distressed house, and BMH lot meet the module-1A richness bar.
- No animation or Drive delivery was performed.

## v3 restyle notes (flat doodle pass)

Regenerated 2026-07-12 to pull the v2 richness back to the LOCKED BMH Sticker System (`docs/course-production/visual-bible/02-style-guide.md`): flat 6-8 color fills, no gradients/lighting/shadows/perspective rendering, thick uniform black hand-drawn outlines. Same content/composition as v2, only the rendering technique changed. Model: `nano_banana_pro` (Higgsfield), 3 reference images per generation — the v2 frame itself (content/layout lock), `module-01/m01_LA_s02_bmh-building.png` (style anchor, already-approved flat rendering), and `_anchors/andrea-standing.png` for any frame showing Andrea. Old v2 files archived unchanged in `_v2-full-detail/`; new v3 files saved at the original filenames, 1600x900 RGB PNG.

- `mV0_LV0_s01_house-wide.png`: All v2 elements preserved — Andrea's house within the full neighborhood row, street, sidewalks, driveways, lawns, layered trees, neighboring houses, clouds. Restyle flattened the previously-shaded tree canopies to solid rounded silhouette blobs and removed the soft roof/lawn shading. Sky pixel-verified and corrected to canonical `#62b3f3` via flood-fill (was landing ~7-13 units off per channel out of the generator, std ~0 confirming flat fill, no gradient). No elements dropped.
- `mV0_LV0_s02_keys-detail.png`: Andrea's hand, yellow cuff, orange door, cream trim, and porch foliage all preserved. Hand skin pixel-verified pure white (~254,254,254). Sky sliver in the top-left corrected to canonical blue. Flat fills confirmed via patch-variance scan (no gradient blending detected anywhere in the frame).
- `mV0_LV0_s03_home-door.png`: Centered orange door, full porch, steps, planters, sidewalk, lawn, neighboring house edges, trees, and clouds all preserved for Match Cut A. Sky corrected to canonical blue; bushes flattened to solid silhouette shapes (previously had visible shading blend).
- `mV0_LV0_s04_walk-to-car.png`: Headset-free Andrea walking past her house/tree toward the continuity orange sedan, full neighborhood context preserved. Andrea's face and hands pixel-verified pure white (~254,254,254), matching the character anchor's flat cylindrical-limb, dot-eye treatment. Sky corrected to canonical blue in 3 disconnected sky regions (behind foreground objects) without bleeding into the neighboring house's blue-toned window (confirmed distinct hue, no false match).
- `mV0_LV0_s05_sidescroll.png`: Continuity sedan in true profile, foreground mailbox and foliage, lane-lined street, and the full row of homes/driveways/sidewalks/lawns/trees/clouds preserved. Sky corrected to canonical blue across 6 disconnected regions. Car body confirmed flat single-tone orange (no gradient) via patch scan.

QC method: automated patch-variance scan across each frame (std < 4 on 24x24 blocks) to confirm flat-fill rendering with no gradients, plus targeted pixel sampling for sky and visible skin. All 5 frames passed on the first generation attempt — no retries needed.

- `mV0_LV0_s06_windshield-pov.png`: All v2 elements preserved — hands-on-wheel driver POV, mirrors (rearview inset + door mirror), pillars, gauges, vents, radio controls, dashboard detail, full neighborhood through the glass. Generated with a third reference (`andrea_car_v2.png`) to hold the interior richness bar. Restyle flattened the previously-shaded gauge faces and dashboard gradient to solid flat panels; gauge needles kept, no numeral/glyph text. Hands sampled pure white (255,255,255), matching the `andrea_car_v2.png` reference hand exactly — no correction needed. Sky/glass recolored to canonical `#62b3f3` via tolerance-based pixel snap (generator was landing ~4-14 units off per channel, consistent flat single tone, no gradient/banding introduced by the correction). Passed on first generation attempt.
- `mV0_LV0_s07_topdown-grid.png`: All v2 elements preserved — full overhead grid, roof ridgelines, gables, chimneys, driveways, yards, sidewalks, intersections, lane lines, the pool + patio table on one lot, green trees, and the one orange car in the intersection. No blank rooftop boxes. Restyle flattened the previously-shaded/textured shingle roofs to solid flat roof colors and removed the ambient-occlusion shading between hedges and lawns. Sky/road-adjacent blue corrected to canonical `#62b3f3`. Passed on first generation attempt.
- `mV0_LV0_s08_notice-door.png`: All v2 elements preserved — full distressed house in neighborhood context, overgrown green yard, cracked walkway, crooked shutter, chimney, neighboring house edges, large tree, clouds. `NOTICE` baked text verified exact and legible. First attempt introduced an unwanted white sticker die-cut halo/outline around the house that wasn't in the v2 reference or the style anchor (which are both full-bleed with no keyline) — regenerated with an explicit no-halo/full-bleed instruction; second attempt is clean, matches the anchor's full-bleed convention. Sky corrected to canonical `#62b3f3`. Took 2 attempts.
- `mV0_LV0_s09_rearview.png`: All v2 elements preserved — distressed house receding inside the orange-trimmed rearview mirror inset, door-mirror inset lower-left, detailed neighborhood road with houses/trees/sidewalks/clouds continuing ahead, dashboard sliver. No glyphs anywhere (distant door paper and gauges stayed blank). First attempt had the same white die-cut halo issue as s08, plus the trees reflected inside the rearview mirror rendered as bare/leafless (autumn-style) instead of matching the green foliage everywhere else in the sequence — regenerated with explicit no-halo and all-trees-green-including-mirror instructions; second attempt is clean on both counts. Sky corrected to canonical `#62b3f3`. Took 2 attempts.

QC method (s06-s09): pixel sampling across each frame for the dominant sky/glass color (median block sampling), tolerance-based snap-to-canonical `#62b3f3` correction applied programmatically (not eyeballed) where the raw generation landed off-canon, then re-verified post-correction. Visual pass for gradient/shading absence, dropped-element check against the v2 content note, and skin-tone sampling for s06's hands.

- `mV0_LV0_s10_bmh-lowangle.png`: All v2 elements preserved — low-angle arrival at the canon module-01 BMH building, orange continuity sedan driving into frame with wheel motion lines, background white and cream parked cars, detailed parking stalls/lane lines, mature trees, bushes, sidewalk, curb, clouds. The v2 render had drifted into a genuine 3D architectural corner (mitered/receding roofline, converging side wall) plus a drop shadow under the sedan and a glossy paint highlight — all removed. Building now reads as a flat elevation-style icon matching the module-01 anchor's exact yellow/orange/cream/blue construction. `B|M|H` sign verified exact and legible. Sky pixel-corrected to canonical `#62b3f3` via global color-distance snap (see method note below) after the flood-fill approach tried first bled color into a tree canopy through anti-aliased gaps and was discarded in favor of the safer global-distance method. Passed on first generation attempt (sky correction was post-processing, not a regeneration).
- `mV0_LV0_s11_follow-entrance.png`: All v2 elements preserved — headset-free Andrea followed from behind (three-quarter back view, mid-stride) across the lot toward the same canon building, orange sedan parked behind her, trees, bushes, sidewalk, clouds. Same 3D-corner/shadow/gloss drift as s10, corrected the same way. Andrea's character identity cross-checked against the `andrea-standing.png` anchor (hair shape, yellow top, cream pants, orange shoes) with the headset explicitly excluded and her pose held to the from-behind walk in the v2 content reference. Visible skin (hands/neck) pixel-verified pure white (~254,254,254). Sky corrected to canonical `#62b3f3`. Passed on first generation attempt.
- `mV0_LV0_s12_bmh-door.png`: All v2 elements preserved — centered Match Cut B, canon yellow facade, orange columns, blue double doors, `B|M|H` sign, green planters both sides, paving, flanking trees, clouds. This frame was already a near-symmetric elevation in v2, so the restyle mainly stripped a subtle shading pass on the walls and a soft/painterly tree-canopy texture, replacing both with flat single-tone fills. Sky corrected to canonical `#62b3f3`. Passed on first generation attempt.
- `mV0_LV0_s13_the-turn.png`: All v2 elements preserved — Andrea headset-free, facing camera with calm resolve, centered in front of the canon entrance. This frame took 3 attempts: attempt 1 scaled Andrea up enough that her head fully covered the `M` in the `B|M|H` sign (only `B` and `H` were legible) — regenerated with an explicit instruction to hold her scale/vertical position so the whole sign stays visible above her head, which fixed the coverage but introduced orange window-frame trim instead of the canon's cream/white trim; attempt 3 corrected the window trim back to cream/white while keeping the sign fully unobstructed. Final frame: `B|M|H` fully visible and exact, building trim matches canon (cream window frames, orange columns/roofline/planters only), face and hands pixel-verified pure white (~253-254 across channels), no drop shadow under her feet. Sky corrected to canonical `#62b3f3`.

QC method (s10-s13): visual crop-and-zoom inspection against the v2 content reference and the module-01 style anchor for perspective/shading/shadow removal and building-identity consistency across all four frames (same yellow/orange/cream/blue construction, same `B|M|H` sign). Sky correction used a global per-pixel color-distance threshold (Euclidean distance < 20 from canonical `#62b3f3`, replaced with the exact canonical value) rather than flood-fill — flood-fill was tried first and leaked through anti-aliased gaps in one tree canopy on s10, corrupting its foliage; the distance-threshold method was verified not to touch door/window blues (which sit ~130+ units away in color distance) or any green foliage before being applied to all four frames. Skin sampled pure white on s11 and s13. Final files resized to 1600x900 RGB PNG to match the sequence standard (generation output was 2752x1536 at the same 16:9 ratio).

## v3.1 continuity fix (2026-07-12)
- `mV0_LV0_s11_follow-entrance.png`: Jarrad flagged that Andrea was staged too far from the parked orange sedan — several car-lengths of gap made it read like she'd walked away from a different vehicle rather than the one she just parked. Regenerated with the same style/content/references, tightening the gap to ~1-2 body-widths so it clearly reads as "just stepped out, first few strides toward the building." Flagged pre-fix version archived at `_v3-continuity-issue/mV0_LV0_s11_follow-entrance_v3-flagged.png`. Sky corrected to canonical `#62b3f3` via color-distance threshold (151,168 px, no leak onto car windows/trees confirmed by visual check). Resized to 1600x900.

## Ending restaged (2026-07-12) — Jarrad direction
Jarrad changed the closing beat blocking: instead of a single "she stops and turns" frame, she now reaches the door, pushes it partially open, turns back over her shoulder for the line, and the shot punches in to a close-up on her face for the delivery — then cuts straight to the title card (no extra "walks in" frame; keeps the ending punchy per the established cinematic-minimalism direction).
- `mV0_LV0_s13_door-ajar-turn.png` (NEW, replaces old s13 "the-turn"): medium shot, door visibly cracked open, Andrea's hand on the door edge, body/head turned back toward camera mid-pause. Matches s12's environment exactly (flanking trees, clouds, canon building) — this also fixes the tree/cloud inconsistency Jarrad flagged between the old s12 and s13 (old s13 had no trees/clouds; the old flagged version is archived at `_v3-restaged-superseded/mV0_LV0_s13_the-turn_pre-restage.png`).
- `mV0_LV0_s13b_closeup-line.png` (NEW): tight close-up on her face/shoulders for the line delivery itself — the "zoom in" beat. Flat background crop of the building's yellow wall/sky, no blur/bokeh (style has no depth-of-field). Skin pixel-verified pure white (254-255).
Both sky-corrected to canonical `#62b3f3`, resized to 1600x900, no leak onto building/hair/skin (visually confirmed).

## Skin-color defect fix (2026-07-12, Jarrad caught it)
`mV0_LV0_s13_door-ajar-turn.png` shipped with Andrea's face/hand rendered as tan/brown (~173,106,55) instead of pure white — the model didn't hold the skin instruction on this generation, and it was NOT pixel-verified before delivery (only visually eyeballed, which is exactly the failure mode rule 1g exists to prevent). Fixed via color-distance flood correction (tan seed → pure white 250,251,250), 14,236px corrected, residual-scanned to 0, and cross-checked against tree-trunk/outline colors to confirm no bleed. Defective original archived at `_v3-restaged-superseded/mV0_LV0_s13_door-ajar-turn_SKIN-DEFECT.png`. Going forward: every character frame in this storyboard gets a pixel skin-check before delivery, independent of whether the full render-QC harness is on or paused.

## Close-up frame redo — off-brand fix (2026-07-12, Jarrad caught it)
The first close-up (`_v3-restaged-superseded/mV0_LV0_s13b_closeup-line_OFFBRAND-macro-crop.png`) was an edge-to-edge macro crop — hair filled almost the whole frame, no negative space, read as a photographic close-up rather than a sticker. Redone as a closer medium shot: complete rounded head/shoulders silhouette, generous flat blue negative space, a hint of the BMH sign for context. Attempt 1 of the redo introduced an unwanted white die-cut sticker halo around her (same class of defect flagged and fixed on s08/s09 earlier) — removed via a black-outline-bounded flood fill (absorbed the halo + anti-aliased blend pixels into canonical blue, verified it did not touch her white face fill or yellow shirt). Skin pixel-verified pure white (255,255,255) post-fix. Resized to 1600x900.

## Ending simplified (2026-07-12, Jarrad decision)
The separate close-up frame (13b) is DROPPED from the storyboard. Jarrad confirmed the door-ajar frame (`mV0_LV0_s13_door-ajar-turn.png`) is the single ending frame — she delivers the full closing line here, and the "zoom to her face" is a CAMERA/MOTION instruction for the animation pass (a push-in on this frame during line 5), not a second illustration. The redone close-up is archived at `_v3-restaged-superseded/mV0_LV0_s13b_closeup-line_DROPPED-zoom-is-motion.png` (may be useful as a zoom-target reference for the animation model). Storyboard is back to 13 frames.

## s10 animation vertical-stretch defect fix (2026-07-13, Jarrad caught it)
`_animated/mV0_LV0_s10.mp4` originally passed the animation-pass QC (`_animated/ANIMATION-REPORT.md`: "0 cuts, 0 freezes — building rises convincingly") but Jarrad flagged it visually as "weird... the building is stretching vertically." The original QC method (cut/freeze detection, skin sampling, single-frame style checks) didn't include a proportion check across frames, so it missed a progressive vertical warp on the "low angle rising" camera move — confirmed by eye on the extracted frames (door/sign visibly elongated by the final frame vs. the first).

Regenerated on the SAME approved still (`mV0_LV0_s10_bmh-lowangle.png`, unchanged) via `generate_video` (Higgsfield MCP, model `seedance_2_0`, 5s, 720p, std mode, silent) with an explicit rigidity-correction prompt: gentle/subtle rise, low-angle held, and an instruction that the building is a rigid flat 2D illustration whose architecture (straight vertical/horizontal lines, window/door/sign proportions) must not deform, stretch, or warp — only the camera moves.

**Verification:** extracted 9 evenly-spaced frames (0/12/25/37/50/62/75/87/100%) and measured the `B|M|H` sign's bounding box per frame via a Python/PIL+scipy connected-component color-distance scan (target ~RGB(27,78,56), tol 20, largest connected blob). Result: sign width held rock-steady at 237-238px across all 9 frames; height varied only 95-110px (h/w ratio 0.40-0.47, no progressive trend — just minor edge/compression noise). The sign moves down-and-shrinks slightly in frame as the camera rises (expected for a rising low-angle move), but its own proportions do not change. Re-ran the same measurement script against the archived defective clip for comparison: width swung 102-251px and height 90-178px with a non-monotonic, noisy ratio (0.46-1.09) — consistent with genuine shape distortion, not measurement noise. Also ran standard checks: 0 scene cuts (ffmpeg scenedetect >0.35), 0 freeze flags (freezedetect -30dB/0.5s), no visible style drift (flat fills held, no added shading/gradients), no characters in frame so no skin check applicable. Output format matches the sequence standard (1280x720, h264, yuv420p, 24fps).

**Verdict: clean fix, first attempt.** No second attempt was needed. Defective original archived at `_animated/_superseded/mV0_LV0_s10_BUILDING-STRETCH-DEFECT.mp4`; fixed clip now lives at `_animated/mV0_LV0_s10.mp4`.

## s11 animation trajectory fix (2026-07-13, Jarrad caught it)
`mV0_LV0_s11.mp4` (animation, not the still — `mV0_LV0_s11_follow-entrance.png` is unchanged/approved): Jarrad flagged that the animated clip read as Andrea "walking past her car, not from it" — the retried v1 clip (already 1 retry deep from the original animation pass, fixing an earlier in-place walk-cycle defect) kept her at a roughly constant lateral offset from the parked orange sedan for most of the shot, so the apparent recession of the car in frame was driven mostly by camera pull-back rather than her actually departing it.

Regenerated via Higgsfield `generate_video` (`seedance_2_0`, same approved still as sole image reference, 4s/720p) with an explicit blocking correction: she begins at the car having just stepped out, then walks a clear diagonal path AWAY from the car toward the BMH entrance, with the car required to recede/shrink in frame and her trajectory required to diverge from (not run parallel to) the car's position. One attempt was needed (did not require the up-to-2-attempt allowance).

Verification: 6 evenly-spaced proof frames (roughly 0/20/40/60/80/100 pct through the clip) reviewed frame by frame. Frame-by-frame trajectory read: frame 1 (0 pct) — she's immediately adjacent to the car, at the rear passenger door, reading as "just stepped out"; frame 2 (~20 pct) — she's advanced about a body-width past the car's rear, a small pavement gap has opened; frame 3 (~40 pct) — a clear grey-pavement gap has opened between her and the car's rear bumper, and she's now angled more toward the building entrance; frame 4 (~60 pct) — the car has shrunk in frame and sits toward the left edge, the gap is substantially larger, the building fills much more of frame; frame 5 (~80 pct) — the car is smaller still and more cropped at the left, she continues on a diagonal path toward the building's front walk, clearly separated from the car by a wide stretch of lot pavement; frame 6 (~100 pct) — the car is small and pushed to the left edge, she's near the building's sidewalk, squarely oriented toward the entrance doors. The gap between her and the car grows continuously and her path visibly diverges toward the entrance rather than tracking parallel to the car at a fixed offset — this reads as departing the car, not passing it.

Also ran standard checks against the same 6 frames: no freeze flags (ffmpeg freezedetect -30dB/0.5s clean), no scene cuts (ffmpeg scenedetect >0.35 clean — rules out the original v1 "walking in place" defect recurring), strong net motion confirmed by pixel diff (mean_abs_diff 78.3 between first and last proof frame, well above the near-static freezedetect false-positive range of ~2-9 seen elsewhere in this sequence). Hands/gloves pixel-sampled in the closing frame: 1399 near-white px vs 34 cream/tan px in the hand region (99.6 pct white, no skin-tone regression). Flat doodle style held throughout — no shading, no gradients, no 3D lighting drift — and the BMH signage stays legible in every sampled frame. Automated car/person bounding-box tracking was attempted for a numeric gap measurement but was abandoned as unreliable (the BMH signage/columns and building trim share the same orange/yellow palette as the car and her shirt, producing noisy false-positive blobs); the frame-by-frame visual read above is the verification of record.

Old defective clip (the "walking past the car" version, itself already a retry over the original "walking in place" v1) archived at `_animated/_superseded/mV0_LV0_s11_WALKING-PAST-CAR-DEFECT.mp4`.

**Verdict: confirmed clean fix, not just best-available** — the trajectory now reads unambiguously as departing the car for the entrance, achieved on the first regeneration attempt of this pass (did not need the second allowed attempt).

## s04 driver's-side entry fix (2026-07-13, Jarrad caught it)
Both `mV0_LV0_s04_walk-to-car.png` (still) and `_animated/mV0_LV0_s04.mp4` needed a fix: Jarrad flagged "Andrea's getting into the wrong side of her car on number four. She's getting into the passenger seat, not the driver's seat."

Checked the car's established orientation first against `mV0_LV0_s05_sidescroll.png` and `mV0_LV0_s06_windshield-pov.png` (the continuity orange sedan): both already show the steering wheel on the car's near/camera side, front seat — so the car's orientation itself was correct and consistent across the sequence; no need to flip or re-block the car. The defect was in the animated clip's blocking: extracted frames from the original `mV0_LV0_s04.mp4` showed Andrea opening the REAR door and sitting in the back seat (the steering wheel visible, empty, in the front compartment ahead of her) — the still only showed her generically approaching the car from the rear-left, which left the img2video model free to pick the nearest (rear) door.

**Still fix:** regenerated via `nano_banana_pro` (Higgsfield MCP), 3 references (the original s04 still for content/layout lock, `_anchors/andrea-standing.png` for character identity, `mV0_LV0_s05_sidescroll.png` to hold the car's established orientation), explicit prompt to reposition her a few steps further along so she's directly beside the FRONT door with her hand on the front handle next to the visible steering wheel, not the rear door. Same neighborhood/house/tree/sky/car preserved. Sky corrected to canonical `#62b3f3` via global color-distance threshold (dist<30, ~567k px, verified no leak onto the blue neighboring house — house wall/roof sit ~145 units away in color distance, well clear of the threshold). Skin pixel-sampled pure white on both face (255,255,255 dominant in a tight face-only patch) and hand (255,255,255 dominant) — no correction needed, passed clean on first generation. Resized to 1600x900. Old still archived at `_v4-driverside-fix-superseded/mV0_LV0_s04_walk-to-car_v3-passenger-side-defect.png`.

**Animation fix:** regenerated via `generate_video` (`seedance_2_0`, std/720p/16:9/silent, 4s, matching the sequence standard), sole reference the new fixed still, explicit prompt that she opens the front driver's door and sits directly in the driver's seat behind the wheel, must not enter via the back seat/rear door. One attempt, no retry needed.

**Verification:** extracted 5 evenly-spaced frames. Frame-by-frame read: she stands beside the front door (steering wheel visible right beside her hand) → opens the front door → sits down directly behind the wheel, both hands reaching it by the final frame → no B-pillar between her and the wheel, back seat empty throughout. This is the fix confirmed by direct observation, not just the prompt's intent. Also ran: 0 scene cuts (ffmpeg scenedetect >0.35), freezedetect flagged a `freeze_start: 0` with no `freeze_end`/duration reported (same near-static-opening false-positive pattern seen elsewhere in this sequence) — confirmed real motion via pixel diff across all 5 frames (mean_abs_diff 7.5 → 5.9 → 4.3 → 3.1, continuously declining as the door-close/settle motion slows, never zero). Skin pixel-sampled on the final frame: face patch and hand-on-wheel patch both dominated by 255,255,255 / 248,248,248-range whites, no tan/cream drift. Flat-fill style held throughout, no gradients/shading introduced. Output format matches the sequence standard (1280x720, h264). Old defective clip archived at `_animated/_superseded/mV0_LV0_s04_v1-passenger-side-defect.mp4`.

**Verdict: confirmed clean fix.** She now unambiguously enters and sits in the driver's seat; the car's orientation is unchanged and remains consistent with s05/s06.

## s05 missing-driver fix (2026-07-13, Jarrad caught it)
Both `mV0_LV0_s05_sidescroll.png` (still) and `_animated/mV0_LV0_s05.mp4` shipped with the car empty — Jarrad: "I don't see anyone driving in the car on number five." The original s05 composition (3-layer parallax: foreground mailbox/tree line, row of houses, sky/rooftop layer) never included Andrea in the driver's seat at all; this was a content gap in the original still, not a drift introduced later.

**Still fix:** regenerated via `nano_banana_pro` (Higgsfield MCP), 3 references — the original s05 still (content/layout lock, critical for preserving the parallax composition), `_anchors/andrea-standing.png` (character identity: dark curly hair, pure white skin, yellow top, no headset), and `module-01/andrea_car_v2.png` (driving-interior reference, used only as a posture/hand-grip guide since this shot stays in exterior side-profile, not interior POV). Explicit prompt: reproduce the reference exactly (same houses, tree, mailbox, clouds, lane-lined street, canonical blue sky) and ONLY add Andrea visible through the driver's-side window, hands on the wheel, facing forward. First attempt landed clean: background composition confirmed unchanged by side-by-side visual comparison (same house count/positions/tree/mailbox/clouds), Andrea on-model (dot eyes, curly black hair, yellow top, no headset), one hand clearly gripping the wheel (the other naturally occluded by her body at this profile angle — consistent with the shot's camera angle, not a defect). Skin pixel-sampled pure white (250-255 range) on first generation, no correction needed. Sky corrected to canonical `#62b3f3` via color-distance snap (dist<20, ~728k px, verified against window-glass blue which sits ~49+ units away — no bleed risk). Resized to 1600x900. Old driverless still archived at `_v3-restaged-superseded/mV0_LV0_s05_sidescroll_NO-DRIVER-superseded.png`.

**Animation fix:** regenerated via `generate_video` (`seedance_2_0`, std/720p/16:9/silent, 6s — matching the original shipped clip's duration), sole reference the new fixed still, prompt reused the original's winning lateral-tracking spirit ("steady lateral tracking, car drives left-to-right, background parallax") plus an explicit instruction that Andrea must stay visible through the window with hands on the wheel for the full clip, not duck out of frame or get occluded. One attempt, no retry needed.

**Verification:** extracted 5 evenly-spaced frames (0/25/50/75/97%). Andrea is clearly and consistently visible driving in ALL 5 frames — same face, same hand-on-wheel grip, same position relative to the window in every frame, confirmed by direct visual inspection of zoomed crops, not just the prompt's intent. Parallax background confirmed intact and rich throughout: mailbox/tree foreground, full row of houses with driveways/lawns/bushes, clouds, and the large foreground tree branch all present and shifting at different rates frame-to-frame (genuine parallax, not flattened). Skin pixel-sampled in a face-region window across all 5 frames: white-pixel count (288-315) dominated cream-pixel count (16-46) in every frame, consistent with the pass threshold used elsewhere in this sequence (h264 compression AA accounts for the small cream fraction). ffmpeg checks: 0 scene cuts (scenedetect >0.35), 0 freeze flags (freezedetect -30dB/0.5s). Style-flatness spot-checked (sky/road patch variance) with no gradient drift beyond expected background-element movement through the fixed sample window. Output format matches the sequence standard exactly (1280x720, h264, yuv420p, 6.04s). Old driverless clip archived at `_animated/_superseded/mV0_LV0_s05_NO-DRIVER-DEFECT.mp4`.

## s01 + s02 cold-open content fixes (2026-07-13, Jarrad reviewed the animated cut)

Two separate complaints, both content-level (not drift/QC misses like the fixes above):

**s01 house-wide — Andrea now visibly exits the house.** Jarrad: "don't we need to see her coming out of the house for number one?" The approved still had never included Andrea at all — extreme wide of the house/neighborhood only. Regenerated via `nano_banana_pro` (Higgsfield MCP), 2 references — the existing s01 still (content/layout lock: same canon 3-window house, street, sidewalks, driveways, lawns, layered trees, neighboring houses, clouds — explicitly instructed NOT to redesign any of it) and `_anchors/andrea-standing.png` (character identity). Added Andrea stepping through the open front doorway, one hand on the door edge, one foot on the porch step, small in frame matching the extreme-wide composition. First attempt landed clean: house/street/neighborhood confirmed unchanged by visual comparison against the prior still, Andrea on-model (dark curly hair, yellow sweater, cream pants, orange shoes, dot eyes, comma nose, no headset). Sky corrected to canonical `#62b3f3` via color-distance threshold (dist<30, ~1.13M px corrected — next-closest non-sky color in the frame, the road gray, sits ~101 units away, no bleed risk). Skin pixel-verified pure white: face patch dominant color exactly 255,255,255; hand-on-doorknob patch dominant ~251,251,251 (both in tight, outline-excluded sample windows). Resized to 1600x900. Old still archived at `_v4-coldopen-fixes-superseded/mV0_LV0_s01_house-wide_NO-ANDREA-EXIT.png`.

Animated via `generate_video` (`seedance_2_0`, std/720p/16:9/silent, 5s — matching the original clip's duration), sole reference the new fixed still, prompt: slow push-in while she completes the exit (steps down onto the porch, releases the door, door swings closed behind her, a couple of steps toward the street) within the same clip. One attempt, no retry needed. Verification: 5 evenly-spaced proof frames (0/25/50/75/97%) — frame 1 shows her still at the doorway with the door open and her hand on its edge; frame 5 shows her standing clear on the front walk, door now closed behind her, confirming the exit plays out over the clip rather than being a static held pose. 0 scene cuts (ffmpeg scenedetect >0.35), 0 freeze flags (freezedetect -30dB/0.5s). Face pixel-sampled pure white (255,255,255 dominant) on the final frame. Flat-fill style held throughout, no gradients/shading introduced. Output format matches the sequence standard (1280x720, h264, yuv420p, 5.04s). Old clip archived at `_animated/_v4-coldopen-fixes-superseded/mV0_LV0_s01_NO-ANDREA-EXIT.mp4`.

**Verdict: confirmed fix** — she is now unambiguously shown exiting the house, addressing the complaint directly.

**s02 keys-detail — full repurpose to ignition key-turn.** Jarrad: "I don't understand what the purpose of this clip is. Should she be inserting them into the ignition of the car?" This wasn't a drift fix — the shot's entire content changed. The old disconnected keys-in-hand-at-the-door insert is dropped; replaced with a tight close-up inside the continuity orange sedan: a white-skinned hand gripping a key seated in the ignition barrel, steering wheel and dashboard (gauge cluster, vents, radio controls) visible, matching the interior richness/style already established in `mV0_LV0_s06_windshield-pov.png` and `module-01/andrea_car_v2.png`. Regenerated via `nano_banana_pro`, 2 references — `andrea_car_v2.png` (interior/dash richness reference) and `mV0_LV0_s06_windshield-pov.png` (the already-approved car-interior style treatment for this exact vehicle). First attempt landed clean: steering wheel, dashboard, radio, and vents all present in the established flat style, no gauge numerals or stray glyphs. A small house is visible through the window in the upper-left for street continuity. Sky/glass patch corrected to canonical `#62b3f3` (initial 30-unit threshold caught 0px on this frame since the raw sky landed ~41 units off canon — widened to 45-unit threshold for this specific correction after confirming the next-closest non-sky color in the frame sits 275+ units away, no bleed risk; ~181k px corrected). Skin pixel-verified: hand region 100% pure white (255,255,255 / 254,254,254 dominant, zero non-white pixels in a broad sample). Resized to 1600x900. Old still archived at `_v4-coldopen-fixes-superseded/mV0_LV0_s02_keys-detail_ORIGINAL-DISCONNECTED-INSERT.png`.

Animated via `generate_video` (`seedance_2_0`, std/720p/16:9/silent, 4s — Seedance's duration floor, same constraint noted for s02/s03/s12 in the original animation pass; task's ~2s guide isn't achievable), sole reference the new fixed still, prompt: quarter-turn clockwise key-start motion, small wobble/motion-lines around the ignition barrel, camera holds steady. One attempt, no retry needed. Verification: 5 evenly-spaced proof frames — visible key/wrist rotation from frame 1 to frame 5 (the key head's angle and the hand's grip visibly turn), steering wheel and dashboard held static as scripted, no stray text/numerals introduced. 0 scene cuts, 0 freeze flags. Hand pixel-sampled: 82% of a broad sample window ≥245 on all channels, with 254,254,254/255,255,255 the two most common colors by a wide margin (the non-white minority is dashboard-orange bleeding into the sample box edges, not skin drift). Flat-fill style held throughout. Output format matches the sequence standard (1280x720, h264, yuv420p, 4.04s). Old clip archived at `_animated/_v4-coldopen-fixes-superseded/mV0_LV0_s02_ORIGINAL-KEYS-INSERT.mp4`.

**Verdict: confirmed fix** — the shot now unambiguously reads as starting the car via the ignition, addressing the complaint directly.

**Sequencing note (asset filenames unchanged):** s02's new content plays 4th in the final edit (after s04, walk-to-car), not in its original position 2. See the `Playback order` column and the "COLD-OPEN FIXES" section added to `docs/course-production/shotlists/videozero-cold-open-scenecards.md` — VO line 1's tail needs re-timing to land over s03 (door-close match cut) instead, flagged there for the later VO/assembly pass.

**Verdict: confirmed clean fix, first attempt on both still and animation.** Andrea is now unambiguously driving the car in s05, and the 3-layer parallax richness (the whole reason this shot exists in the shot list) is fully preserved.