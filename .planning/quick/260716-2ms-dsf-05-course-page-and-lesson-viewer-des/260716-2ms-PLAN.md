---
quick_task: 260716-2ms
title: DSF-05 course page and lesson viewer design-system reskin
type: execute
autonomous: true
branch: codex/design-system-05-course-lesson
must_haves:
  truths:
    - The learner course page presents real module, lesson type, completion, progress, and prerequisite lock data in the canonical BMH course layout.
    - Content lessons use the canonical back link, Baloo title, real meta chips, content stage, previous and next navigation, completion action, and sticky chapter rail with todo, done, locked, active, and completed-count states without changing completion, sanitization, signed URL, or role-play token behavior.
    - Video, text, pdf, image, audio, download, external_link, embed, role_play, divider, and callout blocks each have a coherent BMH visual treatment and no new content type or lesson content is invented.
    - Kit details that have no real data source, including authored duration metadata and a key-points model, are reported as data gaps rather than fabricated.
    - Quiz and assignment runner inner markup, server actions, src/lib, and middleware remain unchanged.
    - Verification, build, and three 1280x800 learner screenshot proofs pass before the unmerged PR is opened.
  artifacts:
    - path: src/app/(dashboard)/courses/[courseId]/page.tsx
      provides: BMH module and lesson list with real progress and lock states
    - path: src/app/(dashboard)/lessons/[lessonId]/page.tsx
      provides: BMH content lesson shell, navigation, and chapter rail
    - path: src/app/(dashboard)/lessons/[lessonId]/lesson-chapters.tsx
      provides: Lesson-local client navigation adapter composed from the existing ChapterItem
    - path: src/components/content-blocks.tsx
      provides: BMH treatments for all 11 existing content block types
    - path: src/components/video-block-player.tsx
      provides: BMH video stage while preserving 90 percent watch tracking
    - path: ._dsf05-proofs/
      provides: Untracked course, video lesson, and content-heavy lesson screenshots
  key_links:
    - from: src/app/(dashboard)/lessons/[lessonId]/page.tsx
      to: src/components/content-blocks.tsx
      via: ContentBlockRenderer for content lesson rows
    - from: src/components/content-blocks.tsx
      to: src/components/video-block-player.tsx
      via: uploaded video block rendering
    - from: src/app/(dashboard)/lessons/[lessonId]/page.tsx
      to: src/app/(dashboard)/lessons/[lessonId]/mark-complete-button.tsx
      via: existing lesson completion action surface
---

# DSF-05 course page and lesson viewer design-system reskin

## Test strategy

Bootstrap completed before coding with `git fetch origin && git merge origin/main --no-edit` and `npm install`. The design audit covers `03-kit.png`, `03-loop.png`, `02-loop.png`, `02-locked.png`, `screens.jsx`, `screens2.jsx`, `data.js`, and `readme.md`. Update RTL and rendering contracts before behavior. Cover the course page's real progress, lock, and type states. Cover video, text, pdf, image, audio, download, external_link, embed, role_play, divider, and callout plus their critical security or behavior attributes. Cover chapter navigation semantics and preserve the video player's 90 percent completion trigger.

## Tasks

1. Write the DSF-05 presentation contracts first.
   - files: `src/app/(dashboard)/courses/[courseId]/page.test.ts`, `src/app/(dashboard)/lessons/[lessonId]/lesson-chapters.test.tsx`, `src/components/content-blocks.test.tsx`, `src/components/video-block-player.test.tsx`
   - action: Add focused expectations for the BMH course hierarchy, mixed completion and lock states, all 11 named content blocks, secure iframe attributes, semantic linked and locked chapter rows, all ChapterItem states, the completed counter, and unchanged 90 percent video completion behavior. Confirm the new visual contracts fail on the old markup while existing behavior assertions remain green.
   - verify: Run the four focused test files and record the expected pre-implementation failures.
   - done: Tests prove the requested presentation without weakening security or completion tracking.

2. Reskin the course and content lesson surfaces.
   - files: `src/app/(dashboard)/courses/[courseId]/page.tsx`, `src/app/(dashboard)/lessons/[lessonId]/page.tsx`, `src/app/(dashboard)/lessons/[lessonId]/lesson-chapters.tsx`, `src/app/(dashboard)/lessons/[lessonId]/mark-complete-button.tsx`, `src/components/content-blocks.tsx`, `src/components/video-block-player.tsx`
   - action: Compose BMH Card, Badge, ProgressBar, Button, and the existing ChapterItem where their contracts fit. Keep the shared BMH primitives unchanged. Add only read-only presentation data needed for the real meta chips, chapter rail, completed counter, and previous or next links. Give uploaded video blocks the canonical outlined dark stage, play overlay, and duration chip derived from the media element's real metadata while retaining native controls and tracking. Preserve every existing query purpose, action, sanitizer boundary, signed URL flow, embed sandbox, role-play token flow, and 90 percent video tracking. Keep quiz and assignment runner markup unchanged. Record authored duration metadata and the key-points card as data gaps because the real lesson schema has neither source. Do not invent them.
   - verify: Focused tests pass. Run the React accessibility and performance checklist. Review the diff for logic changes, route drift, and scope creep.
   - done: Course and content lesson pages match the canonical references across desktop and narrow layouts while all existing learning behavior remains intact.

3. Verify and deliver the unmerged PR.
   - files: `.planning/quick/260716-2ms-dsf-05-course-page-and-lesson-viewer-des/260716-2ms-SUMMARY.md`, `.planning/quick/260716-2ms-dsf-05-course-page-and-lesson-viewer-des/260716-2ms-VERIFICATION.md`, `.planning/STATE.md`
   - action: Run `npm run verify`, `npm run build`, scoped lint, and `git diff --check`. Use Playwright at 1280x800 with the service-account 1Password flow without printing fixture passwords. Use the Browser V1 learner fixture to capture a mixed-state course page, a video lesson, and a content-heavy lesson exercising as many block types as the seeded data offers into untracked `._dsf05-proofs/`. Perform an adversarial manual diff review. Commit coherent units, push, and open the exact requested PR without merging or requesting Claude review.
   - verify: GitHub reports an open PR from this branch to `main`; proof PNGs stay untracked; the PR body lists screens, actual block coverage, component gaps, and verification results; the PR is not merged.
   - done: All requested gates pass and the pushed PR is ready for the orchestrating review.
