---
quick_task: 260715-vx9
title: DSF-01 design-system foundation
type: execute
autonomous: true
branch: codex/design-system-01-foundation
files_modified:
  - src/app/globals.css
  - src/app/globals.test.ts
  - src/app/layout.tsx
  - src/styles/bmh-ds/colors.css
  - src/styles/bmh-ds/typography.css
  - src/styles/bmh-ds/spacing.css
  - src/styles/bmh-ds/foundation-contract.fixture.ts
  - src/styles/bmh-ds/foundation.test.ts
  - public/brand/mascot/*.png
must_haves:
  truths:
    - The exact BMH Institute color, typography, and spacing variables are available without importing Google Fonts CSS or applying base element styles.
    - Baloo 2 and Nunito Sans are self-hosted through next/font CSS variables while existing routes continue to use their current Geist-driven font styling.
    - All 14 Andrea mascot sprites are public at /brand/mascot/ with their source filenames unchanged.
    - Existing Sandra tokens and existing route styling remain intact. Any colliding BMH variable is prefixed and documented.
    - Verification and build pass, the branch is pushed, and an unmerged PR targets main.
  artifacts:
    - path: src/styles/bmh-ds/colors.css
      provides: Exact BMH color variables with collision-safe names where required
    - path: src/styles/bmh-ds/typography.css
      provides: Exact type scale and font-role variables wired to next/font variables
    - path: src/styles/bmh-ds/spacing.css
      provides: Exact spacing, radius, border, shadow, and motion variables
    - path: src/app/layout.tsx
      provides: Baloo 2 and Nunito Sans next/font loaders and CSS-variable classes
    - path: src/styles/bmh-ds/foundation-contract.fixture.ts
      provides: Repo-local expected token declarations, collision mappings, font contract, mascot filenames, and source SHA-256 hashes
    - path: src/styles/bmh-ds/foundation.test.ts
      provides: Self-contained CI drift tests for the committed design-system foundation
    - path: public/brand/mascot/
      provides: Fourteen pose, face, and logo-head PNG assets
  key_links:
    - from: src/app/globals.css
      to: src/styles/bmh-ds/*.css
      via: CSS imports after the existing shared token imports
    - from: src/styles/bmh-ds/typography.css
      to: src/app/layout.tsx
      via: --font-display and --font-body reference the CSS variables emitted by next/font
    - from: public/brand/mascot/*.png
      to: course-assets/design system/assets/mascot/*.png
      via: exact filename and byte-for-byte copy, excluding sheets/
---

# DSF-01 design-system foundation

## Goal

Add the new BMH Institute design tokens, font resources, and Andrea mascot files without changing the rendered appearance of any existing route. Work only in this worktree and leave the PR open for adversarial review.

## Source and constraints

- Source root: `/Users/jarradhenry/Sites/BMH apps/BMH Institute/course-assets/design system/`.
- Read the installed Next.js 16 font guide at `node_modules/next/dist/docs/01-app/03-api-reference/02-components/font.md` before editing `layout.tsx`.
- Port values from `tokens/colors.css`, `tokens/typography.css`, and `tokens/spacing.css`. Do not port `tokens/base.css` or the Google Fonts `@import` from `tokens/fonts.css`.
- Preserve `@sandra/tokens/theme.css`, the Geist loaders, and all existing HTML/body classes. Adding font variable classes is allowed. Applying Baloo 2 or Nunito Sans to an element is not.
- Before implementation, recompute collisions against the imported Sandra CSS, Tailwind theme CSS, and app CSS. The execution-time scan found twelve: `--font-mono`, `--radius-xs`, `--radius-sm`, `--radius-md`, `--radius-lg`, `--radius-xl`, `--radius-2xl`, `--shadow-xs`, `--shadow-sm`, `--shadow-md`, `--shadow-lg`, and `--ease-out`. Rename only confirmed collisions to `--bmh-*`, add an adjacent comment mapping each original name, update internal references if any, and list every rename in the PR body.
- Additive-only gate: no existing component, route, theme value, global element rule, dependency, or shared token import may be removed or restyled.
- No-merge gate: push and open the PR, then stop. Do not merge it.

## Test strategy

Use contract-first drift tests because this slice exposes static resources rather than behavior. First add a committed repo-local fixture and make the tests fail on the missing files and imports. The fixture must contain the complete expected declaration map for all 131 variables after approved transforms, the complete original-to-prefixed collision map, the two next/font family/weight/CSS-variable contracts, the exact 14-file mascot manifest, and the SHA-256 hash expected for each mascot PNG. CI tests may read only committed repo files and this committed fixture. They must never read or require `/Users/jarradhenry/Sites/BMH apps/BMH Institute/course-assets/design system/`. Cover preservation of the Sandra import, absence of `base.css` and Google Fonts CSS imports, exact token declarations, next/font wiring, unchanged Geist consumption, exact mascot filenames, and destination hashes. Separately during execution, compare the port against the external source mechanically, allowing only documented collision renames and the two next/font variable substitutions.

<tasks>

<task id="1" title="Lock and port the token and font contracts">
  <files>src/app/globals.test.ts, src/app/globals.css, src/app/layout.tsx, src/styles/bmh-ds/colors.css, src/styles/bmh-ds/typography.css, src/styles/bmh-ds/spacing.css, src/styles/bmh-ds/foundation-contract.fixture.ts, src/styles/bmh-ds/foundation.test.ts</files>
  <action>Build the self-contained fixture first from the reviewed source: commit every expected variable name and normalized value, all approved collision mappings, and the exact Baloo 2 and Nunito Sans loader contracts. Write failing static contract tests that compare committed CSS and layout source only against that fixture. Do not encode an external absolute path in test code. Recompute all declared CSS variable collisions against `@sandra/tokens/theme.css` and existing app CSS during execution. Copy the three source token files with exact values and names except confirmed collisions, which receive `--bmh-` names plus mapping comments. Replace only the `--font-display` and `--font-body` family heads as needed so they reference dedicated next/font variables while retaining the source fallback stacks. Import all three local files from `globals.css` without removing or reordering away the live shared-token import. In `layout.tsx`, add `Baloo_2` at weights 500, 600, 700, 800 and `Nunito_Sans` at weights 400, 600, 700, 800 with latin subsets and dedicated CSS variables. Add their variable classes to the existing html class list while preserving both Geist loaders and current route font consumption.</action>
  <verify>Run the focused Vitest contract tests in a context where the external source directory is unavailable. Separately run a normalized `diff -u` or equivalent declaration comparison against the three external source files and review every difference. Confirm no committed test or fixture contains or reads the external absolute source path. Confirm no local CSS imports `tokens/base.css`, `tokens/fonts.css`, or a fonts.googleapis.com URL.</verify>
  <done>All 131 source variables are represented with exact values, every collision and font contract is committed in the repo-local fixture and tested without external files, font resources exist as unused CSS variables, and no global element selector or current font class changed.</done>
</task>

<task id="2" title="Add and verify the Andrea mascot asset set">
  <files>public/brand/mascot/face-content.png, public/brand/mascot/face-curious.png, public/brand/mascot/face-laugh.png, public/brand/mascot/face-neutral.png, public/brand/mascot/face-smile.png, public/brand/mascot/face-thinking.png, public/brand/mascot/face-worried.png, public/brand/mascot/logo-head.png, public/brand/mascot/pose-hips.png, public/brand/mascot/pose-point.png, public/brand/mascot/pose-present.png, public/brand/mascot/pose-stand.png, public/brand/mascot/pose-thinking.png, public/brand/mascot/pose-wave.png, src/styles/bmh-ds/foundation-contract.fixture.ts, src/styles/bmh-ds/foundation.test.ts</files>
  <action>Before copying, calculate each source PNG SHA-256 and commit the exact filename-to-hash map plus the 14-name manifest in the repo-local fixture. Write the failing destination manifest and hash assertions against that fixture. Copy the 14 top-level PNG files from `assets/mascot/` into `public/brand/mascot/` with filenames unchanged. Exclude `sheets/` and all non-PNG files. The passing test must calculate hashes from destination files only and compare them with the committed source hashes.</action>
  <verify>Run the focused contract test with no access to the external source. Separately compare sorted external source and destination filename manifests and use execution-time checksums to prove each destination PNG is byte-identical to its source. Confirm the committed hash fixture contains 14 unique filenames and 14 valid SHA-256 values.</verify>
  <done>The destination contains exactly the seven face sprites, six pose sprites, and `logo-head.png`, with no sheet assets, and CI proves their content from committed hashes alone.</done>
</task>

<task id="3" title="Prove additive delivery and open the review PR">
  <files>All DSF-01 files above plus git commits and PR metadata</files>
  <action>Install dependencies only if the current install is missing or stale. Run full verification and build. Review `git diff` and generated output adversarially for any visual side effect. Create atomic commits split by coherent value, push `codex/design-system-01-foundation`, and open a PR to `main` titled `Design-system foundation: BMH Institute tokens, fonts, mascot assets`. The PR body must list the three token files, both font families and weights, all mascot assets as a 14-file set, every collision rename, focused/full verification results, build result, and the additive-only visual proof. Do not merge.</action>
  <verify>`npm run verify` passes. `npm run build` passes. `git diff origin/main...HEAD -- src/app src/styles public/brand/mascot` shows only the additive resources, contract updates, CSS imports, and unused font-variable setup. `gh pr view --json url,baseRefName,headRefName,state` reports an OPEN PR from the requested branch to main.</verify>
  <done>The pushed atomic commits have a green verify and build, the PR body fully records renames and proof, the PR remains open, and no existing route styling was changed.</done>
</task>

</tasks>

## Final gate

Return `FINAL VERDICT [DSF-01]: DONE — <PR URL>` only after all checks and the open PR are confirmed. Otherwise return the BLOCKED form with the concrete blocker. Nothing in this plan authorizes merging.
