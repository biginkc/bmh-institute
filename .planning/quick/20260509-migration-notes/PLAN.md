# Migration notes

## Goal

Capture BMH Institute migration facts before the standalone repo is moved into the BMH Platform monorepo.

## Scope

- Read the platform migration planning docs.
- Inventory this repo's package manager, runtime, framework, scripts, env vars, CI, Supabase usage, scheduled jobs, and path-sensitive details.
- Add a top-level `MIGRATION-NOTES.md`.

## Verification

- Review the note against repo files.
- Run `npm run verify` before commit.
