---
quick_task: 260716-33v
title: DSF-06 quiz and assignment runner reskin
type: execute
autonomous: true
branch: codex/design-system-06-quiz-assignment
files_modified:
  - src/app/(dashboard)/lessons/[lessonId]/quiz-runner.tsx
  - src/app/(dashboard)/lessons/[lessonId]/quiz-runner.test.tsx
  - src/app/(dashboard)/lessons/[lessonId]/quiz-gate-card.tsx
  - src/app/(dashboard)/lessons/[lessonId]/quiz-gate-card.test.tsx
  - src/app/(dashboard)/lessons/[lessonId]/assignment-runner.tsx
  - src/app/(dashboard)/lessons/[lessonId]/assignment-runner.test.tsx
  - src/app/(dashboard)/lessons/[lessonId]/page.tsx
must_haves:
  truths:
    - Quiz gates, all three question types, scoring, and pass or retry outcomes retain their existing behavior with BMH design-system styling.
    - Assignment text, URL, and file submissions retain their existing behavior and pending, approved, and needs-revision states use the BMH design system.
    - Pass and approval celebrations use Andrea with a smile or laugh. Failed quiz and cooldown states use Andrea's worried face.
    - No server action, library, middleware, lesson content renderer, or course surface changes.
    - Verification and build pass, five requested Playwright proofs exist untracked, and an unmerged PR targets main.
  artifacts:
    - path: src/app/(dashboard)/lessons/[lessonId]/quiz-runner.tsx
      provides: Restyled quiz runner and pass or retry outcomes
    - path: src/app/(dashboard)/lessons/[lessonId]/quiz-gate-card.tsx
      provides: Restyled passed, no-attempts, and cooldown gates
    - path: src/app/(dashboard)/lessons/[lessonId]/assignment-runner.tsx
      provides: Restyled assignment form and review states
    - path: src/app/(dashboard)/lessons/[lessonId]/*.test.tsx
      provides: RTL behavior and state coverage for the restyled markup
---

# DSF-06 quiz and assignment runner reskin

## Goal

Apply the approved BMH Institute visual system to the learner quiz and assignment surfaces while preserving every existing validation, server action call, redirect, and state transition.

## Scope and constraints

- Compose only from `src/components/bmh-ds/` plus existing shared controls where the design-system library has a documented gap.
- Keep the required shared lesson `page.tsx` diff to the import and render handoff for the extracted quiz gate component.
- Do not modify `content-blocks.tsx`, course pages, `src/lib/`, middleware, server actions, or lesson content rendering.
- Treat the user work package as the plan source and stop at an open pushed PR. Do not invoke Claude and do not merge.

## Test strategy

Write RTL coverage before changing behavior. Cover true or false, single choice, and multi-select answer state; incomplete quiz rejection; exact quiz action payload; pass and retry results; gate copy and Andrea emotion; assignment text, URL, and file action payloads; and pending, approved, and needs-revision states including reviewer notes and resubmission. Mock only action, toast, and file-upload boundaries. Keep behavioral assertions independent from decorative implementation details except for requested mascot expressions and design-system state markers.

## Tasks

1. Add failing RTL tests for quiz, gate, and assignment behavior plus the requested visual state contracts.
2. Restyle the three scoped components with BMH `Card`, `Button`, `Badge`, and `Coach` primitives. Extract quiz gates so the shared lesson page changes only at the import and component handoff.
3. Run focused RTL tests, `npm run verify`, and `npm run build`. Review the diff for behavior drift and prohibited files.
4. Use the Browser V1 fixture accounts through the service-account 1Password path. Run the app on port 3221 and save the five requested 1280 by 800 screenshots under untracked `._dsf06-proofs/`.
5. Merge current `origin/main` again, resolve only in-scope conflicts, rerun verification, create atomic commits, push, and open the requested unmerged PR.

## Final gate

Return the DSF-06 DONE verdict only after the open PR and all proof gates are confirmed. Otherwise return the BLOCKED form with the exact blocker.
