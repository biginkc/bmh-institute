---
quick_task: 260901-21f
status: complete
completed: 2026-09-01
implementation_commit: a7177f70
---

# Internal intranet learner quiz-summary API summary

## Delivered

- Added the read-only `GET /api/internal/intranet/learners/[email]/quiz-summary` route for BMH me.
- Added fail-closed bearer authentication through the server-only `INTRANET_SERVICE_TOKEN`. Unset, placeholder, whitespace, short, malformed, and incorrect tokens are rejected before database access. Supplied credentials are compared through fixed-length SHA-256 digests with `timingSafeEqual`.
- Kept service-role access behind the credential guard and limited the route to reads.
- Scoped the final quiz to the published canonical `bmh-employee-training-v1` course. The route fails closed on duplicate canonical courses or an ambiguous terminal quiz.
- Added a stable service-role-only SQL read function that resolves the complete canonical quiz catalog and computes count, best score, and latest completion in one statement snapshot.
- The SQL function rejects duplicate canonical courses, duplicate highest-order modules, and duplicate final lessons. Attempt aggregation is scoped by learner, quiz, and lesson.
- Returns the exact required fields and applies the inclusive 85-point passing boundary.
- Added no-store response headers and allowed only the exact learner quiz-summary path shape through the session middleware. Other routes under the intranet prefix still require a Supabase session.

## Test failure proof

- The initial 501 route stub made all five initial contract tests fail.
- A deliberate auth bypass made the authorization rejection test fail while the summary tests stayed green.
- A deliberate auth lockout made the summary tests fail while the rejection test stayed green.
- The middleware reachability regression test failed before the route prefix was added.
- Duplicate-email and unsafe-token tests failed against the first implementation before the guard and profile lookup were hardened.
- The SQL migration contract started with three expected missing-file failures before the migration existed.
- Fix round 1 replayed the new equality, one-snapshot, and module-tie tests at pre-fix commit `1562413a`. All three failed on the old implementation.
- The fix-round middleware sibling test failed on the broad intranet prefix before the allowlist was narrowed.
- The corrected focused suite passes 18 tests across the route, migration, and middleware files.

## Adversarial review corrections

- Replaced global published-course ordering with canonical import scoping and ambiguity rejection.
- Replaced row-cap-dependent catalog discovery and three separate attempt reads with a one-snapshot SQL read function.
- Added explicit top-module ambiguity rejection so equal sibling order cannot choose an arbitrary final quiz.
- Resolved the terminal module from every module in the canonical course before filtering for quiz lessons. A higher or tied terminal module without a quiz now fails closed instead of silently selecting an earlier quiz module.
- Included completed attempts with null scores in the attempt count and latest timestamp.
- Added lesson ID scoping so a reused quiz cannot mix attempts from another lesson.
- Rejected weak service-token configuration and duplicate case-insensitive learner emails.
- Refactored the route after static analysis found avoidable complexity and test duplication.
- Replaced broad middleware prefix admission with the exact `/api/internal/intranet/learners/[email]/quiz-summary` path shape.
- Corrected the threat model: a caller holding the internal service token can distinguish an unknown learner's 404 from a known learner's 200.

## Verification

- `npm run verify`: passed with 226 unit files and 1,424 tests plus 42 RTL files and 182 tests.
- `npm run build`: passed with Next.js 16.2.11 and emitted the new dynamic route.
- The real-schema integration test passed against the canonical TEST project as `service_role`. It covered literal-asterisk email input, one-snapshot attempt aggregates, a terminal-module tie without a quiz, and a same-module terminal-quiz tie. It installed the candidate SQL function and disposable catalog inside one transaction then rolled back all DDL and fixture rows.
- Scoped ESLint over every changed source and test file: passed.
- `git diff --check`: passed.
- Fallow changed-file audit found no introduced dead code or complexity. It warned on conventional duplication in the generated Supabase type file and migration contract tests.
- Exact-commit data and security re-reviews reported no remaining P0, P1, or P2 blocker after the one-snapshot migration fix.
- Full-repository ESLint remains red on 14 inherited errors in unrelated course-production files. This branch introduces no lint error.
- Course-content QA still has three unrelated failures: two expired operating-stack assertions and one missing local held-video asset.

## Scope and release state

- The branch includes a read-function migration. It does not mutate learner or quiz data and its execute permission is granted only to `service_role`.
- No production migration application, production configuration, token provisioning, deployment, or merge was performed.
- No durable TEST migration was applied. The integration test rollback was confirmed by an absent function and zero disposable learner profiles after the run.
- The implementation commit is `a7177f704733913bf136cd8b8dcbf022a6cb419c`.
