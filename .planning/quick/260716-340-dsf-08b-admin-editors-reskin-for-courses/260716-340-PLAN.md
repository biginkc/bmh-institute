---
quick_task: 260716-340
title: DSF-08B admin editors reskin for courses, lessons, and programs
type: execute
autonomous: true
branch: codex/design-system-08b-admin-editors
must_haves:
  truths:
    - Course and program lists, create forms, edit forms, attachments, order controls, and the course modules editor match the BMH admin kit while preserving existing data and actions.
    - The lesson editor matches the kit across Details, Content blocks, Quiz, and Assignment presentation, and the content palette keeps all 11 existing block types reachable.
    - Existing validation, redirects, queries, server actions, upload behavior, and dnd-kit drag-and-drop or reorder handlers remain unchanged.
    - Only the user-listed admin courses, lesson editor, programs presentation files and their RTL tests change; shared components and every excluded admin lane remain untouched.
    - Verification, build, and four requested 1280x800 Playwright proofs pass before an open unmerged PR is created.
  artifacts:
    - path: src/app/(dashboard)/admin/courses/
      provides: Reskinned course list, new and edit forms, modules and lessons editor
    - path: src/app/(dashboard)/admin/lessons/[lessonId]/edit/
      provides: Reskinned details, 11-type content palette, quiz, and assignment editors
    - path: src/app/(dashboard)/admin/programs/
      provides: Reskinned program list, new and edit forms, course attachments, and order mode
    - path: src/app/(dashboard)/admin/{courses,lessons,programs}/**/*.test.tsx
      provides: RTL presentation and preserved interaction coverage
    - path: ._dsf08b-proofs/
      provides: Untracked 1280x800 screenshots and port 3224 proof configuration
  key_links:
    - from: src/app/(dashboard)/admin/courses/**
      to: src/components/bmh-ds/
      via: BMH Table, Card, Badge, Button, Input, and IconButton composition
    - from: src/app/(dashboard)/admin/lessons/[lessonId]/edit/**
      to: src/components/bmh-ds/
      via: BMH tab panels, cards, controls, badges, icon buttons, and 11-type palette
    - from: src/app/(dashboard)/admin/programs/**
      to: src/components/bmh-ds/
      via: BMH list, form, attachment, status, and order-mode presentation
    - from: editor client components
      to: existing actions.ts, quiz-actions.ts, and assignment-actions.ts
      via: unchanged imports, payloads, handlers, validation, and reorder semantics
---

# DSF-08B admin editors reskin

## Scope lock

- First run `npm install`, then `git fetch origin && git merge origin/main --no-edit` before tests or implementation. Treat the post-merge editor behavior as the baseline.
- Modify only presentation files under `src/app/(dashboard)/admin/courses`, `src/app/(dashboard)/admin/lessons/[lessonId]/edit`, and `src/app/(dashboard)/admin/programs`, plus co-located `.test.ts` and `.test.tsx` files needed for the same behavior.
- Do not edit any `actions.ts`, `*-actions.ts`, `src/lib/`, middleware, `src/components/bmh-ds/`, or admin overview, users, role-groups, submissions, or reports files. Do not add shared or one-off design-system primitives. Record component gaps in the PR body.
- Follow the canonical `readme.md` and `admin.jsx`, `admin2.jsx`, and `admin3.jsx` references. Preserve all current routes, labels with behavioral meaning, action payloads, confirmations, toasts, upload controls, empty/error states, accessibility semantics, and dnd-kit behavior.

## Tasks

1. Lock the existing editor behavior with failing RTL presentation expectations.
   - files: Co-located `.test.ts` and new `.test.tsx` files only under the three scoped admin directories.
   - action: After the required origin merge, add tests before changing markup. Cover course and program list counts/status/links, form labels and success/error/pending states, module and lesson controls, program attachments and order mode, lesson Details, Quiz, and Assignment controls, and all 11 add-block labels. Mock existing actions without changing their contracts and assert representative reorder, attach/detach, save, add, and delete calls still use the existing payloads.
   - verify: Run the focused unit and RTL files. New visual-contract assertions fail against the old markup for presentation reasons while preserved interaction assertions pass or establish the unchanged baseline.
   - done: Tests describe the new BMH markup and prove the old editor behavior, including accessible icon controls and every content-block type.

2. Reskin the scoped course, lesson, and program surfaces.
   - files: `src/app/(dashboard)/admin/courses/{page.tsx,course-form.tsx,new/page.tsx,[courseId]/edit/page.tsx,[courseId]/edit/modules-editor.tsx}`, `src/app/(dashboard)/admin/lessons/[lessonId]/edit/{page.tsx,lesson-details-form.tsx,blocks-editor.tsx,quiz-editor.tsx,assignment-editor.tsx}`, `src/app/(dashboard)/admin/programs/{page.tsx,program-form.tsx,new/page.tsx,[programId]/edit/page.tsx,[programId]/edit/course-attachments.tsx}`.
   - action: Recompose only the visual layer from `src/components/bmh-ds/` to match the kit's page heads, tables, cards, status badges, forms, module and lesson rows, tab treatment, question and answer cards, attachment rows, and block palette. Keep the lesson-type gates and existing backed editor panels intact. Preserve all post-merge queries, action imports and calls, state transitions, confirm/toast text, file uploads, redirects, and dnd-kit sensors, IDs, callbacks, and mutation order.
   - verify: Focused unit and RTL tests pass. Review `git diff --check` and the full diff against the post-merge baseline. Confirm no excluded path changed and no behavior-bearing handler or action payload drifted.
   - done: Every requested screen uses the BMH visual language, all 11 palette actions remain reachable, quiz/assignment/details presentation is covered, and all editor operations retain their original behavior.

3. Rebase the slice on current main, prove it, and open the unmerged PR.
   - files: Scoped source/tests above, untracked `._dsf08b-proofs/`, atomic git commits, and PR metadata.
   - action: Run `git fetch origin && git merge origin/main --no-edit` again, resolve only in-scope overlap, and rerun focused tests. Run `npm run verify`, `npm run build`, and `git diff --check`. Using the service-account 1Password flow, load the Browser V1 fixture account JSON without printing passwords and use its `OWNER_*` credentials. Run Playwright against a dev server and untracked proof config on port 3224. Capture 1280x800 course list, modules editor, lesson content tab with all 11 palette types plus one other tab, and program editor screenshots into `._dsf08b-proofs/`. Create coherent atomic commits, push, and open a PR to `main` titled `Admin editors (courses, lessons, programs): BMH design-system reskin`. List screens, component gaps, verification results, and any out-of-scope files touched, which should be none. Do not merge or seek Claude review.
   - verify: Full verification and build are green; proof PNGs and proof config remain untracked; `git diff origin/main...HEAD --name-only` contains only scoped source, tests, and GSD artifacts; GitHub reports an OPEN PR from the requested branch to `main`.
   - done: The exact unmerged PR is pushed with all requested evidence and is ready for the orchestrating session's review.

## Final gate

Return `FINAL VERDICT [DSF-08B]: DONE — <PR URL>` only after the open PR and all gates are confirmed. Otherwise return the BLOCKED form with the concrete reason. Nothing in this plan authorizes merging.
