---
quick_task: 260729-j49
status: blocked
completed: 2026-07-29
implementation_commit: 59aba35
containment_commit: 9fdd618
---

# Institute Hugo lifecycle boundary summary

## Delivered

- Removed Institute account creation, invite revocation, account deletion, suspend, reactivate, and status-edit controls.
- Removed the dead invite queries, tables, overview tiles, validation module, and date dependency.
- Replaced the add-person form with a Hugo link and shared the configurable Hugo URL across all three call sites.
- Preserved role-group membership and the full role-group course-entitlement administration surface.
- Removed every Institute login-status write.
- Added a fail-closed guard that preserves role-group saves when the role is unchanged and rejects role changes before mutation.
- Kept legacy fixture cleanup and the disabled invite route intact. No migration was created or applied.

## Adversarial decisions

- Confirmed PR head `bee9cfe` already contained current `origin/main` `b27d353` before editing. No rebase or merge was justified.
- Refuted the allowed status pass-through fallback. The legacy combined function rewrites status, so a concurrent Hugo suspend or reactivate could be undone by an Institute role edit.
- Refuted a direct service-role `profiles.system_role` update after exact-head Playwright proved the authenticated write was rejected and compensation removed the newly assigned role group.
- Queried the live production schema through the read-only Management API. It has no role-only synchronization operation and no hidden trigger. `fn_hugo_access_is_active` requires `hugo_access_grants.role` to match `profiles.system_role`. A profile-only update would lock a Hugo-backed person out.
- Refuted using `hugo_apply_access` as a workaround. It is a full service-role lifecycle operation that can reactivate access and alter expiry, which crosses the ownership boundary.
- Contained the unsafe branch by rejecting role changes before any write. This leaves role editing unavailable, so acceptance gate 3 is not met.
- Kept the role-group page-level guard inconsistency out of scope as directed. It remains protected by the admin layout.

## Verification

- `npm run typecheck`: passed.
- `npm run lint`: passed with 0 errors and 13 pre-existing warnings.
- `npm run test`: 204 files and 1,245 tests passed.
- `npm run test:rtl`: 41 files and 163 tests passed.
- Focused containment tests: 13 tests passed.
- Fallow changed-code audit: fail because `admin/users/actions.ts` became statically unused after invite removal. The file is retained because the EXECUTE block explicitly requires keeping `updateUserRole` and `setUserRoleGroups`. No new complexity or duplication finding remains.
- `git diff --check`: passed.
- Runtime-source search found no authentication identity create or delete call. Literal `src` search still finds unrelated integration-test fixture setup and teardown helpers.
- `/admin/role-groups` and `supabase/migrations` are unchanged from `bee9cfe`.
- Exact-head GitHub run `30483016237` passed Verify and all PostgreSQL validation jobs. Seeded Playwright failed one role-group save because the rejected role write triggered compensation. The containment commit addresses that failure path. A new exact-head run is pending push.

## Blocker

- The live database needs one atomic Institute-authorized operation that updates `profiles.system_role` and the matching `hugo_access_grants.role` without reading or writing `profiles.status`.
- Adding that operation requires database work, but this lane expressly prohibits a new migration. Role editing therefore remains blocked for Claude's review.

## Commits

- `59aba35` removes Institute lifecycle ownership.
- `9fdd618` contains the unsafe role-sync path and keeps role-group assignment available when the role is unchanged.
