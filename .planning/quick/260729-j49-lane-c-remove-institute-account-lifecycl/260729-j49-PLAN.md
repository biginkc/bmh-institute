---
quick_task: 260729-j49
title: Remove Institute account lifecycle controls
type: execute
autonomous: true
branch: codex/pr129-refresh-main-20260729
must_haves:
  truths:
    - Institute runtime code cannot create or delete authentication identities or legacy invites.
    - Institute administrators can still edit Institute roles and role-group course access.
    - Institute never reads or writes Hugo-owned login status while saving Institute settings.
    - User administration links to Hugo for adding people and exposes no suspend, reactivate, or delete control.
    - The admin overview and users page contain no legacy invite tiles, queries, tables, or revoke actions.
    - Role-group administration remains byte-for-byte untouched and no migration is added or applied.
  artifacts:
    - path: src/app/(dashboard)/admin/users/actions.ts
      provides: Institute-only role and role-group actions with provisioning and invite revocation removed.
    - path: src/app/(dashboard)/admin/users/[userId]/edit/actions.ts
      provides: Role and role-group save path with no account deletion and no client-controlled status.
    - path: src/app/(dashboard)/admin/users/[userId]/edit/user-edit-form.tsx
      provides: Role and role-group editor without lifecycle controls.
    - path: src/app/(dashboard)/admin/users/invite-form.tsx
      provides: Hugo add-person link instead of an Institute provisioning form.
    - path: src/app/(dashboard)/admin/page.tsx
      provides: Overview without invite queries or tiles.
  key_links:
    - from: src/app/(dashboard)/admin/users/[userId]/edit/user-edit-form.tsx
      to: src/app/(dashboard)/admin/users/[userId]/edit/actions.ts
      via: saveUserSettings sends only Institute-owned role and role-group inputs.
    - from: src/app/(dashboard)/admin/users/invite-form.tsx
      to: src/lib/hugo-url.ts
      via: the add-person anchor uses the shared Hugo URL setting.
---

# Remove Institute account lifecycle controls

## Goal

Make Hugo the only account lifecycle surface while preserving Institute role and course-entitlement editing on PR #129. Work from the existing `bee9cfe` head after confirming it already contains current `origin/main`. Never merge or apply migrations.

## Tasks

<task type="auto" tdd="true">
  <name>Task 1: Lock the Hugo boundary with failing tests</name>
  <files>src/lib/security/hugo-provisioning-boundary.test.ts, src/app/(dashboard)/admin/users/page.test.ts, src/app/(dashboard)/admin/page.test.ts, src/app/(dashboard)/admin/users/[userId]/edit/user-edit-form.test.tsx, src/app/(dashboard)/admin/users/[userId]/edit/actions.test.ts, src/app/(dashboard)/admin/users/[userId]/edit/save-settings.test.ts, src/lib/hugo-url.test.ts</files>
  <action>Update focused tests first to require no Institute provisioning, invite revocation, suspend, reactivate, or delete surface; require the save path to omit status completely; require Hugo links; and require invite overview/users surfaces to disappear. Run the focused suites and record the expected red result before implementation.</action>
  <verify>Focused Vitest and RTL commands fail against the old behavior for the intended assertions.</verify>
  <done>The tests describe the new ownership boundary and fail for the old runtime.</done>
</task>

<task type="auto">
  <name>Task 2: Remove lifecycle mutations and dead invite UI</name>
  <files>src/app/(dashboard)/admin/users/actions.ts, src/app/(dashboard)/admin/users/actions.test.ts, src/app/(dashboard)/admin/users/invite-form.tsx, src/app/(dashboard)/admin/users/page.tsx, src/app/(dashboard)/admin/users/users-tables.tsx, src/app/(dashboard)/admin/users/revoke-invite-button.tsx, src/app/(dashboard)/admin/users/[userId]/edit/actions.ts, src/app/(dashboard)/admin/users/[userId]/edit/actions.integration.test.ts, src/app/(dashboard)/admin/users/[userId]/edit/page.tsx, src/app/(dashboard)/admin/users/[userId]/edit/user-edit-form.tsx, src/app/(dashboard)/admin/page.tsx, src/lib/invites/validate.ts, src/lib/invites/validate.test.ts, src/lib/learner-access/status.ts, src/lib/learner-access/status.test.ts, src/lib/hugo-url.ts, src/app/(dashboard)/dashboard/page.tsx, .env.example</files>
  <action>Delete the create-user, rollback-delete, revoke-invite, and account-delete paths plus their dead tests and components. Remove invite queries, rows, tables, and overview tiles. Simplify learner-access shaping to profiles only. Remove lifecycle props and controls from the user editor. Replace the status-writing legacy `fn_save_user_settings` call with a direct `system_role` update and the existing status-free `fn_set_user_role_groups` function. Add one shared Hugo URL helper and use it for all three links. Do not touch fixture cleanup, the disabled legacy apply-invite route, role-group administration, or migrations.</action>
  <verify>Focused suites pass, runtime-source searches find no auth admin create/delete call or status mutation, `git diff` shows no role-group or migration changes, and the retained role/group action tests pass.</verify>
  <done>Institute owns only post-login roles and course entitlements while Hugo owns every account lifecycle operation.</done>
</task>

<task type="auto">
  <name>Task 3: Verify, review, commit, and push the PR head</name>
  <files>All scoped files and GSD evidence artifacts</files>
  <action>Run typecheck, lint, unit, and RTL suites. Review the complete diff adversarially. Record the literal acceptance-gate caveat that test fixtures under `src` still use admin auth helpers while runtime code does not. Commit only scoped changes, push to PR #129's existing head branch, wait for exact-head checks, and report without merging.</action>
  <verify>`npm run typecheck`, `npm run lint`, `npm run test`, and `npm run test:rtl` pass; GitHub reports PR #129 at the pushed head with required checks green; no migration is created; role-group files are unchanged.</verify>
  <done>Claude receives a command-backed REPORT for the pushed unmerged head.</done>
</task>
