---
quick_task: 260729-o39
title: Re-enable Institute system-role editing
type: execute
autonomous: true
branch: codex/institute-reenable-role-edit
must_haves:
  truths:
    - The per-user editor persists an existing user's system role and role groups in one save path.
    - A failed role write restores the previous role groups.
    - An owner cannot downgrade their own role from either role-editing surface.
    - The users-list action continues to persist role changes independently.
    - No migration file changes.
  artifacts:
    - path: src/app/(dashboard)/admin/users/[userId]/edit/actions.ts
      provides: Institute-owned role and role-group persistence with compensation.
    - path: src/app/(dashboard)/admin/users/[userId]/edit/actions.test.ts
      provides: Role-change, role-group, rollback, and self-demotion coverage.
    - path: src/app/(dashboard)/admin/users/actions.test.ts
      provides: Standalone list-page role-edit coverage.
  key_links:
    - from: src/app/(dashboard)/admin/users/[userId]/edit/actions.ts
      to: profiles.system_role
      via: exact service-role profile update after role-group persistence and requireAdmin.
    - from: src/app/(dashboard)/admin/users/[userId]/edit/actions.ts
      to: fn_set_user_role_groups
      via: existing guarded role-group RPC and compensating rollback.
---

# Re-enable Institute system-role editing

## Goal

Restore the Institute-owned role write removed by the temporary Hugo role-equality
guard. Preserve self-demotion protection, role-group enrollment diffing, release
control error normalization, and compensation if the role write fails. Do not
touch database migrations and do not merge.

## Tasks

<task type="auto" tdd="true">
  <name>Task 1: Replace the obsolete block assertion with role-persistence coverage</name>
  <files>src/app/(dashboard)/admin/users/[userId]/edit/actions.test.ts, src/lib/security/hugo-provisioning-boundary.test.ts</files>
  <action>Update tests first so a learner-to-admin change must persist both the role groups and profile role. Preserve tests for compensation and self-demotion. Update the Hugo boundary contract to allow Institute role writes while continuing to forbid login-status writes.</action>
  <verify>Focused Vitest fails against the guarded baseline for the intended role-persistence assertion.</verify>
  <done>The tests reject both the obsolete block and a false-success path that does not write system_role.</done>
</task>

<task type="auto">
  <name>Task 2: Restore the pre-guard persistence path</name>
  <files>src/app/(dashboard)/admin/users/[userId]/edit/actions.ts</files>
  <action>Remove confirmSystemRoleIsUnchanged and restore the profile role update plus role-group compensation. Use the exact service-role update required by migration 025 after requireAdmin. Keep all existing email diffing, error normalization, revalidation, and self-demotion behavior.</action>
  <verify>Focused per-user and list-page action tests pass.</verify>
  <done>Both role-editing surfaces persist roles consistently and preserve their self-demotion guards.</done>
</task>

<task type="auto">
  <name>Task 3: Verify, review, commit, push, and open an unmerged PR</name>
  <files>All scoped source, test, and GSD evidence files</files>
  <action>Run typecheck, lint, unit, and RTL suites. Review the complete diff adversarially. Prove no migration changed. Commit and push the task branch, open a PR, and stop without merging.</action>
  <verify>All requested commands pass, git diff checks pass, the migration diff is empty, and GitHub shows the PR at the pushed head.</verify>
  <done>Claude receives an evidence-backed REPORT for an open unmerged PR.</done>
</task>
