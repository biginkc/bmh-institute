---
quick_task: 260716-1nc
title: DSF-04 app shell and learner dashboard reskin
type: execute
autonomous: true
branch: codex/design-system-04-shell-dashboard
must_haves:
  truths:
    - The shared signed-in shell matches the canonical loop-series left rail and top bar while preserving auth, suspension, role, pending-submission, and sign-out behavior.
    - The learner dashboard presents the existing assigned programs, courses, required-lesson progress, and resume target through the committed BMH design-system components.
    - Learners do not see admin navigation and admins retain every existing admin destination and pending-submission count.
    - Existing dashboard pages remain usable inside the reskinned shell without routing or server-action changes.
    - Verification, build, and learner plus admin 1280x800 screenshot proofs pass before the unmerged PR is opened.
  artifacts:
    - path: src/app/(dashboard)/layout.tsx
      provides: BMH signed-in shell
    - path: src/app/(dashboard)/sidebar-nav.tsx
      provides: Learner and role-gated admin navigation
    - path: src/components/page-header.tsx
      provides: Shared BMH page heading treatment
    - path: src/app/(dashboard)/dashboard/page.tsx
      provides: Data-driven learner dashboard hero, program rail, and lesson grid
    - path: ._dsf04-proofs/
      provides: Untracked learner and admin dashboard screenshots
---

# DSF-04 app shell and learner dashboard reskin

## Test strategy

Update the existing layout source contract, sidebar RTL coverage, and async dashboard rendering tests before changing behavior. Preserve assertions for nav destinations, learner/admin role gating, pending counts, sign-out action, resume target, course progress, error, and empty states.

## Tasks

1. Update shell and dashboard tests first.
   - files: `src/app/(dashboard)/layout.test.ts`, `src/app/(dashboard)/sidebar-nav.test.tsx`, `src/app/(dashboard)/dashboard/page.test.ts`
   - action: Replace styling-only expectations with the canonical BMH shell and dashboard presentation contracts while retaining the existing behavior assertions.
   - verify: Run the three focused test files and confirm the new expectations fail against the old UI for visual-contract reasons.
   - done: Tests cover logo, search, learner/admin navigation, sign-out, data-driven resume/progress, and empty/error states.

2. Reskin the shared shell and learner dashboard.
   - files: `src/app/(dashboard)/layout.tsx`, `src/app/(dashboard)/sidebar-nav.tsx`, `src/components/page-header.tsx`, `src/app/(dashboard)/dashboard/page.tsx`
   - action: Compose the loop-series rail, search top bar, user card, page headings, sky-blue coach hero, yellow progress, program rail, and lesson grid from `src/components/bmh-ds/`. Keep all queries, auth and role checks, route targets, walkthrough mount, and server behavior unchanged. Do not edit `src/lib/`, middleware, or server actions.
   - verify: Focused tests pass. Review the diff for logic and scope drift. Run the React accessibility and performance checklist.
   - done: The requested surfaces match the canonical references and all existing signed-in destinations remain functional.

3. Verify and deliver the unmerged PR.
   - files: `.planning/quick/260716-1nc-dsf-04-app-shell-and-learner-dashboard-d/260716-1nc-SUMMARY.md`, `.planning/quick/260716-1nc-dsf-04-app-shell-and-learner-dashboard-d/260716-1nc-VERIFICATION.md`, `.planning/STATE.md`
   - action: Run `npm run verify`, `npm run build`, and `git diff --check`. Capture learner and admin dashboard screenshots at 1280x800 into untracked `._dsf04-proofs/`. Commit coherent units, push, and create the exact requested PR with component gaps and verification results.
   - verify: GitHub reports an open PR from this branch to `main`; proof PNGs are untracked; the PR is not merged.
   - done: All gates pass and the pushed PR is ready for the orchestrating review.
