---
quick_task: 260716-0xa
title: DSF-03 auth screens reskin
type: execute
autonomous: true
branch: codex/design-system-03-auth
files_modified:
  - src/app/(auth)/login/
  - src/app/(auth)/forgot-password/
  - src/app/auth/set-password/
must_haves:
  truths:
    - Login, forgot-password, set-password, suspended, and ordinary login error states match the BMH Institute auth kit.
    - Screens compose from src/components/bmh-ds without reimplementing its primitives.
    - Auth actions, redirects, validation, rate limits, and middleware remain unchanged.
    - Labels, focus order, and live error announcements remain accessible.
    - RTL coverage proves the new markup while existing action tests continue to prove behavior.
    - Verification, build, and five 1280x800 screenshot proofs pass before an unmerged PR is opened.
  artifacts:
    - path: src/app/(auth)/login/page.tsx
      provides: Split login and suspended/error visual states
    - path: src/app/(auth)/forgot-password/page.tsx
      provides: Reset request and success visual states
    - path: src/app/auth/set-password/page.tsx
      provides: Authenticated password setup visual shell
    - path: src/app/auth/set-password/set-password-form.tsx
      provides: Password setup form using BMH design-system controls
    - path: ._dsf03-proofs/
      provides: Untracked 1280x800 Playwright screenshots
---

# DSF-03 auth screens reskin

## Goal

Replace only the auth screens' visual layer with the committed BMH design system. Preserve all existing auth behavior and deliver an open unmerged PR titled `Auth screens: BMH design-system reskin`.

## Source and constraints

- Canonical UI source: `course-assets/design system/ui_kits/course-platform/auth.jsx`, `screens.jsx`, `readme.md`, and screenshots in the untouched main checkout.
- Use `Button`, `Input`, `Card`, `Coach`, `Logo`, `SpeechBubble`, and `Mascot` from `src/components/bmh-ds/` where the screen needs them.
- Do not edit server actions, middleware, auth routing, or `src/lib/`.
- Preserve the meaning of existing error and success messages.
- Keep labels, native form order, autocomplete metadata, and error announcement semantics.
- Store proof PNGs only in the untracked or ignored `._dsf03-proofs/` directory.
- Push and open the requested PR. Do not merge and do not seek Claude review.

## Test strategy

Add RTL page/form coverage before changing markup. Assert the split login brand copy and controls, an ordinary error announcement, the dedicated suspended notice, forgot-password request and success states, and set-password labels and constraints. Run existing action tests unchanged to prove auth behavior is untouched.

## Tasks

1. Add failing RTL expectations for all requested auth states.
2. Implement the smallest visual composition that satisfies the kit using the existing BMH primitives.
3. Run focused RTL, all action tests, `npm run verify`, `npm run build`, and `git diff --check`.
4. Capture login, login error, forgot-password, set-password, and suspended at 1280x800 with Playwright into `._dsf03-proofs/`.
5. Record verification, make coherent atomic commits, push, and open the exact unmerged PR.

## Final gate

Return DONE only after GitHub confirms the requested PR is open from `codex/design-system-03-auth` to `main`. Do not merge.
