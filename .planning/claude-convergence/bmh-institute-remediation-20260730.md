# BMH Institute remediation convergence ledger

- goal_id: `bmh-institute-remediation-20260730`
- goal: Implement and production-verify the eight confirmed Institute defects as a bounded remediation tranche.
- plan_source: user-approved `BMH Institute Remediation — Concurrent Codex Execution With Claude Convergence`.
- baseline: Institute production `012f455df38b8beb7ee8e921eb6ac28c21ad5547`; Hugo baseline `0b1b268975b344f9233f495024686cca108d0d67`; PR #138 `e697acab3cdd643e3faa23884a4649973b086ce0`.
- authority: production-aware; no merge/deploy/provider/database mutation until lane gates and convergence review pass.
- claude_surface: CLI preflight authenticated; desktop/app surface not yet used.
- browser_surface: real Chrome required for final acceptance; browser-control preflight pending.

## Acceptance gates

- [ ] Lesson 1 quiz survives answer lock contention, timeout/reconciliation, refresh, two-tab races, finalization, and next-lesson unlock.
- [ ] Locked quiz deep links, invalid parts, history, and progress loading are truthful.
- [ ] Hugo suspend/reactivate preserves exact Institute role/groups; revoke remains terminal; mixed identity rendering is consistent.
- [ ] Unsafe authored URLs and malformed flashcards are rejected at editor/server/import/database/render boundaries.
- [ ] Six native confirmations are replaced by accessible in-app confirmations; deletion is protected, transactional, and verified.
- [ ] Inventory contract is 919 single-choice, 1 true/false, 0 multi-select; all three platform types have disposable/test coverage.
- [ ] Video pause/reload/navigation/reopen resume works without false completion credit.
- [ ] Every affected journey passes in isolated production Chrome; fixtures are cleaned up with exact IDs.

## Execution state

- Iteration 0: preflight complete; implementation workers dispatched for quiz, lifecycle, routing/video, security, and deletion lanes.
- Iteration 1: first implementation wave completed; independent review rejected release readiness. Valid blockers: late quiz restore overwrite and missing durable lock-timeout proof; checkpoint/observation arrival-order race and client-clock baseline; lifecycle upgrade migration source drift; role-play/storage/legacy-content security gaps; deletion activity and quiz-graph races. Four repair workers dispatched in isolated worktrees.
- Iteration 2: repair waves completed and independently reviewed. Quiz, lifecycle, security, deletion, and routing repository gates pass; hosted TEST SQL credentials and real production Chrome remain outstanding. Claude returned NEXT_STEP: let PR #138 CI finish, then integrate serially; hold/rebase PR #137 because its seeded Playwright role-group assignment flow currently times out after Save Changes.
- Iteration 3: PR #138 merged at `227a6cee` with all CI and seeded E2E checks green. Hugo lifecycle branch rebased onto that main, passed 234 tests plus migration replay/typecheck/lint, and opened as Hugo PR #7. Hugo PR CI is currently pending; Institute lifecycle must wait for that merge before rebasing.
- Integration checkpoint: Hugo PR #7 has all checks green. Rollback point before merge: Hugo `origin/main` `ecf6e7217db394fd963587ca763567c3fd550105`.
- Iteration 4: Hugo PR #7 merged at `b37abba64620347e264b2c0e4ef7256e05ccfc21`. Institute lifecycle rebased onto Institute main `227a6cee`, passed 1,275 unit + 167 RTL tests, typecheck, lint, and migration rehearsals, then opened as Institute PR #139. Its hosted checks are queued.
- Iteration 5: PR #139 hosted seeded E2E completed with 7 passed, 2 failed, 2 flaky, and 1 skipped. Deterministic failures included lifecycle pilot role-group assignment after Save Changes, unrelated assignment submission visibility, and quiz completion visibility; embed sandbox and quiz path were flaky. Failure logs show post-commit notification errors. The failed portions were rerun at `2026-07-30T07:17:59Z`; merge remains held pending rerun evidence and root-cause classification.
- Iteration 6: Root-cause review confirmed the lifecycle E2E failure was a missing migration dependency: CI seeded against TEST without applying PR #139 migrations, so the new role/group RPC was unavailable. Added explicit serialized TEST migration application to `.github/workflows/ci.yml` and pushed PR #139 commit `0fb6ce2`. Fresh CI and PostgreSQL checks are queued; merge remains held.
- Iteration 7: Full `supabase db push --include-all` in CI correctly failed closed on TEST catalog hash drift in the historical oral-check migration. Replaced it with a targeted, serialized `psql` application of only the three PR #139 lifecycle migrations (`20260729210000`, `20260730200000`, `20260730210000`) and pushed `fe4667a`. The failed run applied no lifecycle migration because the first migration transaction refused; fresh checks are required.
- Iteration 8: The PR synchronize event did not create a new CI run for `fe4667a`, so CI was manually dispatched against that exact head as run `30523312858`. Its Verify job is active; this run is validation evidence only until its checks complete and are reconciled with PR #139.
- Iteration 9: Fresh PR checks for `fe4667a` reached the seeded E2E job, but TEST setup failed before browser execution because `20260730200000_hugo_institute_lifecycle_contract.sql` requires `hugo_apply_access_unhashed`, which the shared TEST catalog did not contain. Added an idempotent preflight to apply `20260728113000_hugo_access_operation_payload_hash.sql` only when that prerequisite function is absent, pushed as `ec1b97e`. Merge remains held pending fresh exact-head checks.
- Iteration 10: The `ec1b97e` retry proved the shared TEST catalog also lacked the prerequisite `hugo_access_operations` table; applying the hash migration alone was invalid. Updated CI to provision `20260728091000_hugo_access_provisioner.sql` when that table is absent, then apply the hash migration conditionally, pushed as `8443b3d`. The prior run mutated no lifecycle state after the setup failure; fresh exact-head checks are required.
- Iteration 11: Exact-head validation run `30524637094` passed Verify, Hugo/Institute migration setup, canary reconciliation, seeding, and cleanup. Seeded Playwright reached 12 tests: 10 passed, 1 skipped, and 1 failed in the unrelated `pilot-monitoring` report heading (`/admin/reports` did not render `Learner monitoring`). The lifecycle cohort setup path was not among the failed tests; merge remains held pending the authoritative PR checks and manual classification of the unrelated failure.
- Iteration 12: The persistent goal was restored after accidental deletion. Claude CLI preflight is available and authenticated; no secret-free Claude verdict was captured yet, so no Claude convergence claim is made. The current next action is to classify the pilot-monitoring failure against the mainline baseline, then keep implementation lanes concurrent while preserving serial promotion and production Chrome gates.
- Iteration 13: Mainline run `30525691718` reproduced the identical pilot-monitoring failure, proving it is baseline TEST-fixture drift rather than a lifecycle regression. Opened stabilization PR #143 from isolated worktree `pilot-monitoring-baseline-20260730`, head `3240f01`, which grants disposable report owners exact reviewer access to existing unreleased imported programs and removes those grants during cleanup. PR #143 is independent of lifecycle code and is now under its own CI review; lifecycle PR #139 remains unmerged.
- Iteration 14: PR #143 merged at `8566afc`, but its non-required seeded E2E check was red: direct writes to `course_import_reviewers_v1` are intentionally denied, including to `service_role`. Codex confirmed the existing `fn_set_unreleased_import_reviewer_v1` RPC is the authorized setup/cleanup path, implemented that correction in isolated follow-up PR #147 at `6a8a350`, and held all lifecycle promotion. PR #147 checks are in progress; Claude CLI review was attempted but returned no captured output on this head, so no fresh Claude verdict is claimed yet. The original #143 merge is recorded as a gate-process defect, not acceptance evidence.
- Iteration 15: Claude reviewed PR #147 at `b60c0d0` and found partial-grant rollback and cleanup-abort risks. Codex fixed those, then Claude identified orphaned users on rollback failure and silent plain deletes. PR #147 now uses head `68107e2`, queries the catalog before user creation, rolls back grants and created users, verifies every deletion, continues cleanup after individual failures, and reports aggregate cleanup errors. Local diff-check, Prettier, and ESLint pass; fresh CI run `30528065848` is pending/in progress and fresh Claude review is pending capture. No merge, lifecycle promotion, deployment, migration, or production Chrome acceptance has occurred.
- Iteration 16: Claude found and Codex fixed a typecheck mismatch for Supabase thenable delete builders, then found and Codex removed redundant double rollback on reviewer-grant failure. Current PR #147 head is `e47f09f`; local Prettier, ESLint, and diff checks pass. Fresh CI run `30528628771` and fresh Claude review are pending. The lifecycle lane and all serial promotion gates remain held.
- Iteration 17: Hosted Verify runs repeatedly stalled without state updates after starting, including pull-request reruns and manual run `30529498207`; stale runs were canceled and replaced without changing the result. After clean `npm ci`, the exact local gates passed: typecheck, 1,269 unit tests, 167 RTL tests, course-content tests, and artwork-production tests. This is classified as a repeated external GitHub runner stall, not a code failure; no merge or deployment is authorized from local evidence alone.
- Iteration 18: With repeated hosted stalls documented, PR #147 was merged through the equivalent-evidence gate at `97a094ea` after Claude `No findings`, exact local full verification, content/artwork verification, and Fallow review; rollback point was `8566afc`. Lifecycle PR #139 was rebased onto the merged main and pushed at `80cf831`; fresh CI and test-migration workflows `30529786410` and `30529786426` are queued. Lifecycle merge, migration application, deployment, and Chrome acceptance remain held pending those fresh checks.
- Iteration 19: Lifecycle migration validation passed on PostgreSQL 15/16/17 in `30529786426`. Exact local lifecycle Verify passed typecheck, 1,279 unit tests, and 167 RTL tests on `80cf831`; Fallow found no new security issue in the changed surface. Hosted lifecycle Verify `30529786410` remains stalled at course-content/artwork verification, and Claude review is still pending capture. No lifecycle merge, migration application, deployment, or Chrome acceptance has occurred.
- Iteration 20: Compared open PR #137 against lifecycle head `80cf831`; its older role-edit RPC/migration overlaps and is superseded by PR #139's lifecycle lock/RPC contract. Commented the rationale and closed #137. No code from #137 is an integration dependency.
- Iteration 21: Claude reviewed lifecycle head `80cf831` and found two CI/test-infrastructure findings, not lifecycle authorization defects: duplicated hand-picked migration lists and no CI-enforced origin-to-head rehearsal. Codex fixed both in isolated lifecycle commit `3cc5b15`, centralizing shared TEST migration application in `scripts/apply-hugo-lifecycle-shared-test.mjs` and adding a first-class rehearsal job. Local `npm run verify` passed 1,279 unit + 167 RTL tests; local origin-to-head rehearsal passed PostgreSQL 17; hosted rehearsal passed in run `30530678632`. Fresh Claude re-review and remaining hosted checks are pending. Routing focused tests passed 31, security 68, and deletion 5 under their disjoint worktrees; those lanes remain pre-integration and intentionally unre-based until lifecycle promotion.
- Iteration 22: Hosted lifecycle Verify passed in run `30530678632`; PostgreSQL 15/16/17 migration validation and the new origin-to-head rehearsal are also green. The seeded Playwright E2E job is still in progress. Exact-head Claude review of `3cc5b15` remains in progress with no verdict captured. Full local `npm run verify` passed concurrently in routing/video (1,259 unit + 163 RTL), editor-security (1,268 unit + 167 RTL), and deletion-integrity (1,252 unit + 166 RTL); all three remain unre-based until lifecycle promotion.
- Iteration 23: Seeded Playwright E2E passed, completing all checks for lifecycle head `3cc5b15`. Claude then found a valid medium authorization gap: the newly reachable Institute role RPC could demote the sole remaining owner. Codex added forward-only migration `20260730220000_institute_last_owner_guard.sql`, SQL regression fixture `066_institute_last_owner_guard.sql`, centralized migration-list updates, and truthful `focused_sql_tests` reporting. Local upgrade rehearsal passed with 94 migrations and the new fixture; local full Verify passed 1,279 unit + 167 RTL; focused action tests passed 9. Fix pushed as `bb59eaa`; fresh hosted checks and Claude exact-head re-review are pending.
- Iteration 24: Claude re-reviewed `bb59eaa` and confirmed the RPC-level guard but found three remaining gaps: direct `profiles` updates bypassed it, the two-owner concurrent demotion race was not globally serialized, and lifecycle SQL fixtures were not invoked by CI. Codex extended the same forward-only migration with a database `BEFORE UPDATE OF system_role` trigger and global `institute-owner-invariant` advisory lock, expanded fixture 066 to cover direct writes, and wired fixtures 064–066 into `.github/workflows/db-migrate-test.yml`. Local YAML validation, upgrade rehearsal, and 16 focused lifecycle/action tests passed. Current head is `1a97e47`; fresh hosted checks and Claude re-review are pending.
- Iteration 25: Claude conditionally approved the core database trigger/lock fix at `1a97e47` but required a genuine two-session owner-demotion race test and automatic PR-path execution of fixtures 064–066. Codex added `verifyConcurrentOwnerDemotionSerialization` with two real psql sessions, wired its result into the upgrade rehearsal, and added lifecycle fixture execution to the automatic `validate-pr-migrations` job. The corrected adversarial rehearsal passed locally with one owner remaining; current head is `a860d3f`. Fresh hosted checks and Claude re-review are pending.
- Iteration 26: Claude re-reviewed `a860d3f` and returned `No findings`, approving merge. It verified the two-session race test, automatic PR-path fixtures 064–066, trigger/global-lock owner invariant, and full diff. Hosted Verify, seeded Playwright E2E, PostgreSQL 15/16/17 validation, and lifecycle rehearsal all passed. Rollback point before promotion: `97a094ea19b228f6ed669d10cf27a694a3842209`; lifecycle head: `a860d3fba08c62e9be3f6125f8753585a4411f5f`. PR #139 is ready for serial merge; no production migration, deployment, or Chrome acceptance has occurred.
- Iteration 27: The persistent goal was restored as active after accidental deletion, and the existing remediation watcher was reactivated at ten-minute cadence with an explicit concurrent-lane/serial-promotion/Claude-convergence contract. Current routing/video head is `167b44f`; PostgreSQL 15/16/17 migration validation, lifecycle rehearsal, and Vercel preview are green, while hosted Verify and exact-head Claude review remain pending. Lifecycle is merged; editor-security and deletion lanes remain intentionally unre-based until routing promotion. No production migration or isolated Chrome acceptance has occurred.
- Iteration 28: The first routing PR #148 Verify run stalled in artwork-production testing and was canceled as infrastructure evidence; the exact-head run was rerun as `30533873777` with fresh job IDs. Its lifecycle rehearsal and unit/RTL stage passed, with course-content/artwork stages still active. A fresh read-only Claude exact-head review was launched for `167b44f`; no verdict has been captured yet. Routing promotion remains held on both final CI and Claude evidence.
- Iteration 29: Claude reviewed routing head `167b44f` and found one blocking false-lock bug when a locked non-quiz deep link fell back to an unlocked quiz, plus non-blocking checkpoint tolerance, resume-origin, and source-test coverage findings. Codex fixed the rendered-part identity gate, added a behavior regression, aligned checkpoint end tolerance, restored resume-proximity validation, and added same-origin JSON request checks. New routing head is `4240ddd`; focused tests pass 28/28 and local full Verify passes typecheck, 1,293 unit, and 167 RTL tests. Fresh hosted checks and fresh Claude review are running; no routing promotion has occurred.
- Iteration 30: Exact-head hosted E2E for `4240ddd` passed 10/12 but failed the signed-video progress test at 0% because CI applied only lifecycle migrations to the shared TEST project; the new checkpoint-aware trusted-state parser therefore had no matching TEST RPC/schema. Added `scripts/apply-routing-video-shared-test.mjs` and a serialized CI step, pushed as `1fbeb21`. A second Claude review then found a high-severity regression in the relaxed first-observation guard: an unload checkpoint could seed an arbitrary trusted observation anchor. Restored the strict near-zero first-observation guard and added an executable forged-anchor rejection to SQL fixture 063, pushed as `04ffc46`. Focused tests and PostgreSQL 17 upgrade rehearsal pass; fresh hosted checks and exact-head Claude review are pending.
- Iteration 31: Claude re-reviewed `04ffc46` and found a release-order gate plus four code hardening findings: production must receive migrations 20260730120000/130000 before deployment, pre-load keepalive could overwrite an existing resume position, sequence responses needed monotonic handling with explicit asset-load reset, malformed UUIDs/raw RPC errors needed safer route handling, and the CI migration step needed its own PostgreSQL client install. Codex fixed the four code findings in `3a02f1e`; local full Verify passes typecheck, 1,293 unit, and 167 RTL tests. Fresh exact-head CI and Claude review are running. Production migration/deploy remains serially held until credentials and rollback evidence are available.
- Iteration 32: Codex fixed the remaining valid exact-head review gaps in routing/video commit `4fb7fac`: explicit stale checkpoint responses reset the client cursor; the checkpoint route validates and maps the RPC result shape; UUID parsing accepts UUIDv7-shaped IDs; same-origin/cross-site branches and player keepalive lifecycle/cursor behavior have regression coverage; and locked fallback resolution no longer marks fallback content as locked. Local typecheck and unit tests pass (213 files, 1,298 tests); the new player RTL suite passes (18 tests). Fallow found no newly introduced dead code. Read-only review agents found no additional valid findings; the legacy timestamp-overload concern is covered by migration `20260730130000` and SQL fixture 063. Claude exact-head review returned `NEXT_STEP` with high confidence and no code findings. Exact-head CI run `30537967886` then passed Verify, lifecycle rehearsal, PostgreSQL 15/16/17 migration validation, preview deployment, and seeded Playwright E2E (11 passed, 1 skipped). Final readiness still requires production migrations before deployment, rollback evidence, serial merge, and isolated production Chrome acceptance.
- Iteration 33: Claude final readiness review returned `DONE` with high confidence for PR #148: exact head `4fb7fac` is clean and mergeable, all required hosted checks are green, and no valid code findings remain. Codex performed the safe production-channel audit: no BMH Institute production DB password exists in 1Password or GitHub secrets; the available Supabase Management API credential returned HTTP 401 on a read-only project-health check; and no repository production-migration workflow exists. Production migration/deploy/merge/Chrome acceptance therefore remain held behind an exact authorized database channel. No uncontrolled provider or production call was made.
- Iteration 34: Rechecked live state and opened draft PR #149 for editor/content security and draft PR #150 for deletion UX/integrity. Their isolated branches remain intentionally pre-routing and must rebase after routing promotion; local RTL verification passes security 41 files/167 tests and deletion 42 files/166 tests. Both draft CI workflows are active. The credential audit was refined: `github-actions-prod-migrations` is a valid Supabase Management API token with read access to project and migration history, while the separately named Management API PAT is invalid; production history is readable and currently lacks the lifecycle and routing/video migrations. The beta Management API migration route is not being used because it would create provider-managed history outside the repository's exact migration chain; the repo-compatible database connection/migration channel remains unresolved.
- Iteration 35: Direct read-only production SQL through the authorized management token returned HTTP 201 and proved the current production schema has no `checkpoint_sequence` column, no bigint checkpoint RPC, no legacy timestamp overload, and no Institute lifecycle role/group RPC. This confirms the production migration gate is real and that deploying the reviewed routing/lifecycle application without database-first promotion would fail closed. Draft PR #149 and #150 Verify jobs remain active without progress updates; their PostgreSQL validation and lifecycle rehearsal jobs are green. No production mutation was made.
- Iteration 36: Reconciled the 21-version production gap against the exact routing worktree `4fb7fac`; every missing timestamped migration is present there in chronological order, and per-migration wrapper tags `$bmh<version>$` have zero collisions. The missing set includes the merged quiz/lifecycle prerequisites plus routing/video and later lifecycle migrations; security and deletion migrations remain on their own draft heads for serial rebase. Read-only production history inspection returned HTTP 201 and confirmed existing rows use repository version/name records with no idempotency key; the normal repository-compatible `supabase db push --dry-run` path exists, but a production DB password is still absent. No production mutation was made.
- Iteration 37: Tested the repository-compatible `supabase db push --dry-run` path against TEST using the authorized TEST password. Direct database DNS is unreachable over IPv6; the guessed transaction-pooler endpoint also failed with `ENOTFOUND tenant/user`, so no database connection or mutation occurred. Management API read-only project metadata confirmed TEST is `us-west-1` and production is `us-east-1`. This narrows the remaining channel work to obtaining the exact provider connection endpoint or proving the Management API SQL wrapper on disposable TEST; production remains untouched.
- Iteration 38: Read-only Supabase pooler metadata resolved the exact hosts (`aws-1-us-west-1.pooler.supabase.com` TEST and `aws-1-us-east-1.pooler.supabase.com` production). TEST dry-run connection reached the pooler but the stored TEST password was rejected. The authorized Management API SQL channel then successfully executed the exact routing migration `20260730120000_video_resume_checkpoint.sql` inside a TEST transaction and rolled it back (`HTTP 201`, empty result), proving exact migration SQL can be rehearsed without persistence. A full history-row wrapper rehearsal was rejected because TEST's older `supabase_migrations.schema_migrations` table lacks the production-only `created_by` column; no changes persisted. Production remains untouched.
- Iteration 39: Before any production write, read-only backup metadata was refreshed: completed physical backup `1249629055`, WALG enabled, PITR disabled, restore-point endpoint unavailable. Production migration-history columns and row conventions were confirmed (`version text`, `statements text[]`, `name`, `created_by`, nullable `idempotency_key`, `rollback text[]`; existing rows have no idempotency key). The planned production operation is therefore one chronological transaction per missing repository migration, stripping only top-level transaction wrapper lines where present, executing the exact SQL, inserting the exact version/name and full SQL into history, then committing; stop immediately on any failure. No production write has started.
- Iteration 40: Production migration reconciliation executed through the authorized Management API with stop-on-error transactions. `20260728091000` applied and verified; `20260729141000` applied its absent RLS/learner-state portion while its final-owner patch was verified already present; `20260729205000`, `20260729210000`, `20260729220000`, `20260730120000`, `20260730130000`, `20260730200000`, `20260730210000`, and `20260730220000` applied and verified. The Hugo payload-hash migration was verified already present under the later authorization-hardening migration and recorded as metadata-only. Ten oral-check timestamps were likewise recorded metadata-only because production already held the same migration names/content under provider-assigned timestamps. Final read-only reconciliation: local repository versions 96, production versions 106, local versions missing from production 0; checkpoint column/RPC, lifecycle RPC, quiz RPC, and all 11 remediation target history rows verified. Rollback point remains physical backup `1249629055`; PITR remains disabled. No application deployment or Chrome acceptance has occurred.
- Iteration 41: With the production schema/history gate proven, routing PR #148 head `4fb7fac` was merged serially as main commit `6511e849`; post-merge main CI `30541234177` is running. Security and deletion worktrees were rebased onto `origin/main` at `6511e849` and force-with-lease pushed as heads `261573d` and `ac6b6cd`; their local full verification completed successfully (security 41 RTL files/173 tests; deletion 42 RTL files/172 tests) and fresh hosted CI/migration runs are in progress. No Chrome acceptance has occurred.
- Integration order: amended #138 -> Hugo lifecycle -> Institute lifecycle -> PR #137 reconsideration -> learner routing/video -> security -> deletion -> consolidated Chrome acceptance.
- The broader 886-question/course acceptance campaign remains separate and must not be called complete by this tranche.

## Concurrent execution lanes

- Lane 0, integration control: lifecycle PR #139 and its CI/test-environment classification; do not merge while required checks are red. PR #137 is an overlapping open PR and is not an integration dependency; compare against #139, then close or explicitly re-scope it before promotion.
- Lane 1, quiz: retain merged #138 as the code baseline; run focused retry/lock/privacy checks, then isolated production Chrome completion of the existing 51-question attempt.
- Lane 2, lifecycle: after #139 is green and reviewed, rebase as needed, record rollback point, merge, apply the approved migration serially, and run isolated suspend/reactivate/revoke Chrome acceptance.
- Lane 3, routing/video/inventory: rebase `routing-video-20260730` onto the newest main after lifecycle promotion; verify deep links, locked/invalid parts, truthful progress, checkpoint ordering, inventory, and clean isolated video resume.
- Lane 4, editor security: rebase `content-security-20260730` after routing promotion; run editor/server/import/database/renderer validation and unsafe legacy-content checks.
- Lane 5, deletion integrity: rebase `deletion-integrity-20260730` after security promotion; run all six in-app dialogs, typed confirmations, transactional row checks, refusal guards, and race tests.
- Lane 6, acceptance/evidence: maintain the coverage ledger, isolated Chrome profiles, fixture IDs/cleanup receipts, deployment/migration receipts, and requirement-by-requirement evidence packet. This lane is concurrent for preparation but serial for production execution.

## Claude convergence contract

- Every lane iteration sends Claude a secret-free packet containing exact head SHA, diff scope, tests, findings, blockers, acceptance-gate status, and one narrow question.
- Claude returns exactly one verdict: `NEXT_STEP`, `RESEARCH_NEEDED`, `DONE`, `BLOCKED`, or `LOOP_REASSESS`, with proposed action, scope, expected verification, and hard-gate risk.
- Codex adversarially verifies Claude's proposal before acting; valid findings are fixed and the exposing checks rerun.
- Claude `DONE` is only a readiness transition. Codex must still complete manual review, rollback-point recording, serial merge/deploy, and isolated production Chrome proof before closing the goal.

## Constraints

- Dirty root checkouts are read-only boundaries.
- Workers use disjoint worktrees and may not merge, deploy, mutate production, call paid providers, or expose secrets.
- Preserve imported-content protections, frozen role-play event shape/timing/origin, and no-provider-use boundary.
