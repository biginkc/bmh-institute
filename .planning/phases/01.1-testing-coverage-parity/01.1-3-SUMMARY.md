---
phase: 01.1-testing-coverage-parity
plan: 3
subsystem: testing
tags: [playwright, vitest, integration, e2e, harden-01, harden-03, harden-04, tpar-03, tpar-05]

requires:
  - phase: 01-auth-and-access-hardening
    provides: HARDEN-01 page-level requireAdmin guards, HARDEN-03 deleteUser action, HARDEN-04 answer_options isolation, integration test files (skipped at end of Phase 01)
provides:
  - HARDEN-01 e2e regression spec at e2e-prod/admin-route-guard-learner.spec.ts
  - integration tests gated by describe.skipIf (skipped cleanly when env missing, run when populated)
  - docs/test-environment-setup.md runbook for .env.test.local population
  - 01-HUMAN-UAT.md retired with closed-with-deferrals status (5 items resolved)
affects: [01.1-1-rtl-test-infrastructure, 01.1-2-playwright-e2e-harness, future write-path coverage decisions, REQUIREMENTS.md TPAR-03 and TPAR-05]

tech-stack:
  added: []
  patterns:
    - "describe.skipIf gating for env-dependent integration tests"
    - "test.use opt-out of storage state for unauthenticated e2e regression"
    - "Path A deferral notation in HUMAN-UAT for destructive items"

key-files:
  created:
    - e2e-prod/admin-route-guard-learner.spec.ts
    - docs/test-environment-setup.md
  modified:
    - src/app/(dashboard)/admin/users/[userId]/edit/actions.integration.test.ts
    - src/app/(dashboard)/lessons/[lessonId]/answer-options-isolation.integration.test.ts
    - .planning/phases/01-auth-and-access-hardening/01-HUMAN-UAT.md

key-decisions:
  - "Decision A locked: in-spec opt-out of storage state via test.use({ storageState: { cookies: [], origins: [] } }) — Option 1, smallest delta, no learner-only account on prod required"
  - "Decision B locked: Path A — destructive HUMAN-UAT items (HARDEN-02 expired-invite teardown, HARDEN-03 UI variant) remain manual until a write-capable test environment exists"
  - "Decision C locked: 01-HUMAN-UAT.md file-level status is closed-with-deferrals"

patterns-established:
  - "describe.skipIf(!envPresent) is the canonical gate for integration tests that require TEST_SUPABASE_* env. Replaces describe.skip (silently never runs) and import-time throw (kills the suite). Reports skipped cleanly when env is missing."
  - "e2e-prod/ specs use test.use({ storageState: { cookies: [], origins: [] } }) inside the describe block to opt OUT of the chromium project's default admin storage state for unauthenticated regressions."
  - "HUMAN-UAT.md deferral note format: result: deferred-until-test-environment with a recorded reason citing the Path lock and the unlock condition (a Path B/C lock by Jarrad)."

requirements-completed: [TPAR-03, TPAR-05]

duration: 7min
completed: 2026-05-01
---

# Phase 01.1 Plan 3: HARDEN/UAT Replacement Summary

HARDEN-01 prod regression now lives in `e2e-prod/admin-route-guard-learner.spec.ts` (4 cases, all green), the two HARDEN-03/HARDEN-04 integration tests are honestly gated via `describe.skipIf`, and `01-HUMAN-UAT.md` is retired as a closed historical record with three items passed via automation, one closed via runbook, and one explicitly deferred under Path A.

## Performance

- Duration: ~7 min
- Started: 2026-05-01T05:30:00Z
- Completed: 2026-05-01T05:37:00Z
- Tasks: 3 (Task 1 inventory + decision lock; Task 2 failing-tests commit; Task 3 documentation commit)
- Files modified: 5 (2 created, 3 modified)

## Accomplishments

- HARDEN-01 (TPAR-03): new prod e2e spec asserts unauthenticated redirect on the four `/admin/reports/*` routes; runs via `npm run test:prod`; passes against the live deployment
- HARDEN-03 integration test re-gated: `describe.skip` replaced with `describe.skipIf(!envPresent)` so the suite runs whenever `TEST_SUPABASE_*` env is populated
- HARDEN-04 integration test re-gated: import-time `throw` replaced with `envPresent` flag plus `describe.skipIf` so `npm run test:integration` no longer crashes when env is unset
- `docs/test-environment-setup.md` is the single canonical reference for populating `.env.test.local` and verifying each test command
- `01-HUMAN-UAT.md` updated: status `partial` to `closed-with-deferrals`, every item has a recorded disposition, no items remain `pending`

## Task Commits

1. Task 1 (inventory + decision lock) — no commit (gate task; produced no files)
2. Task 2 (failing-tests + integration re-gate) — `ad1f12f` (test)
3. Task 3 (env-setup runbook + UAT closure) — `61586fe` (docs)

Order verified: `test(01.1-3):` precedes `docs(01.1-3):` (TDD-honest commit ordering per AGENTS.md).

## Files Created/Modified

- `e2e-prod/admin-route-guard-learner.spec.ts` (created) — HARDEN-01 + TPAR-03 prod regression spec; 4 test cases inside a single `test.describe` that calls `test.use({ storageState: { cookies: [], origins: [] } })` to opt out of the chromium project's default admin storage state
- `src/app/(dashboard)/admin/users/[userId]/edit/actions.integration.test.ts` (modified) — replaced `describe.skip` with `describe.skipIf(!envPresent)`; removed obsolete file-header NOTE block about parallel worktree skipping
- `src/app/(dashboard)/lessons/[lessonId]/answer-options-isolation.integration.test.ts` (modified) — replaced module-level `throw new Error(...)` with `envPresent` flag plus `describe.skipIf`; guarded module-scope admin client construction; added `if (!admin) return;` early returns in three `it` bodies that touch `admin` directly
- `docs/test-environment-setup.md` (created) — runbook for `.env.test.local` setup; documents the three TEST_SUPABASE_* vars, the three E2E_* vars, command sequences for each test suite, and the Path A default
- `.planning/phases/01-auth-and-access-hardening/01-HUMAN-UAT.md` (modified) — file-level status flipped to `closed-with-deferrals`; each test item has a recorded `result:` line; HARDEN-02 marked `deferred-until-test-environment` with Path A reason; Summary block totals reconcile to 5

## Decisions Made

- Decision A (storage-state strategy): Locked Option 1 (in-spec `test.use({ storageState: { cookies: [], origins: [] } })`). Smallest delta, no learner-only account provisioning on prod, asserts the strictest case of HARDEN-01 (unauthenticated redirect to `/login`).
- Decision B (Path A confirmation): Locked Path A. HARDEN-02 expired-invite teardown and the HARDEN-03 UI variant remain manual until a write-capable test environment exists. Path B (Supabase ephemeral branches) and Path C (prod-with-prefix-cleanup) are unlocked alternatives, neither in scope for Phase 01.1.
- Decision C (file-level status label): Locked `closed-with-deferrals`. Accurate and consistent with the Summary block.

## Deviations from Plan

### Auto-fixed Issues

1. [Rule 1 - Bug] Plan-specified Summary totals did not reconcile
- Found during: Task 3 (UAT closure)
- Issue: Plan called for `passed=3, deferred=2, closed=1` which sums to 6 against `total=5`. There are only 5 test items — the HARDEN-03 UI variant is a sub-note under item 3, not a separate item.
- Fix: Set Summary to `passed=3, deferred=1, closed=1` (sums to 5) and added a `sub_deferred: 1` note explaining the HARDEN-03 UI variant counts as a sub-deferral under item 3.
- Files modified: `.planning/phases/01-auth-and-access-hardening/01-HUMAN-UAT.md`
- Verification: total reconciles to passed + deferred + closed + pending = 3 + 1 + 1 + 0 = 5
- Committed in: `61586fe` (Task 3 commit)

2. [Rule 1 - Bug] Plan example URL conflicted with project memory and Playwright config
- Found during: Task 3 (runbook authoring)
- Issue: Plan body suggested `E2E_PROD_BASE_URL=https://university.bmhgroup.com` as the example. Per project memory and `playwright.prod.config.ts` line 47, the live URL is `https://bmh-institute.vercel.app` (no custom domain yet; `university.bmhgroup.com` is aspirational).
- Fix: Used `https://bmh-institute.vercel.app` as the example URL in the runbook to match deployed reality. Per the user prompt, the existing `bmhgroup.com` references in the codebase are NOT changed; the runbook is a new file and uses current truth.
- Files modified: `docs/test-environment-setup.md`
- Verification: matches the default in `playwright.prod.config.ts` and project memory `[BMH Group domain ownership]`
- Committed in: `61586fe` (Task 3 commit)

---

Total deviations: 2 auto-fixed (both Rule 1 - bug fixes against plan-text inconsistencies)
Impact on plan: Both fixes preserve the plan's intent while reconciling against project ground truth. No scope creep.

## Issues Encountered

None. The new spec passed against prod on first run (Phase 01 already shipped the page-level guards). Integration tests skipped cleanly without env. Typecheck and unit suite remained green throughout.

## Verification Results

- `npm run verify` (typecheck + 117 unit tests): exits 0
- `npm run test:integration` (env unset): exits 0 with both `*.integration.test.ts` files reporting `skipped` (was: import-time throw or silent describe.skip)
- `npm run test:prod`: exits 0 with all 12 cases passing (4 new + 8 existing); no regressions
- `git log --oneline`: shows `test(01.1-3): ...` (`ad1f12f`) followed by `docs(01.1-3): ...` (`61586fe`), in order

## User Setup Required

None for this plan. The new spec runs on the existing `npm run test:prod` harness; integration tests skip cleanly when env is unset. The runbook at `docs/test-environment-setup.md` documents what a contributor would need to do to opt INTO running the integration suite locally, but no action is required to land this plan.

## Next Phase Readiness

- TPAR-03 closed: HARDEN-01 has automated regression coverage at the e2e layer
- TPAR-05 closed: each Phase 01 HUMAN-UAT item has a recorded disposition (3 passed via automation, 1 deferred-until-test-environment, 1 closed via runbook)
- 01-HUMAN-UAT.md is now a closed historical record, not a live to-do list
- The two integration tests are ready to run for any contributor who populates `.env.test.local`; the same pattern (`describe.skipIf(!envPresent)`) is the convention for any future integration tests
- Plan 01.1-1 (RTL infrastructure) and Plan 01.1-2 (Playwright e2e harness) remain incomplete; this plan touches `e2e-prod/` and integration tests only, so neither dependency was actually required for this plan's content

## Self-Check: PASSED

- e2e-prod/admin-route-guard-learner.spec.ts: FOUND
- docs/test-environment-setup.md: FOUND
- Modified actions.integration.test.ts (no `describe.skip(`): VERIFIED
- Modified answer-options-isolation.integration.test.ts (no module-level throw): VERIFIED
- 01-HUMAN-UAT.md status `closed-with-deferrals`: VERIFIED
- Commit `ad1f12f` (test): FOUND
- Commit `61586fe` (docs): FOUND
- Commit order: `test(01.1-3):` before `docs(01.1-3):` — VERIFIED

---
*Phase: 01.1-testing-coverage-parity*
*Completed: 2026-05-01*
