---
phase: 01.1-testing-coverage-parity
plan: 2
subsystem: testing
tags: [e2e, playwright, fixtures, prod-ref-guard, tpar-02, path-a]
requirements_completed: [TPAR-02]
dependency_graph:
  requires:
    - playwright.config.ts (already wired, untouched)
    - e2e-prod/auth.setup.ts (selector reference, untouched)
  provides:
    - e2e/fixtures.ts adminClient + ensureTestUser + prod-ref guard
    - e2e/auth.setup.ts one-time login writing storage state
    - e2e/dashboard-smoke.spec.ts first local-dev e2e spec
  affects:
    - .env.example (documents TEST_SUPABASE_* keys for contributors)
tech_stack:
  added: []
  patterns:
    - Service-role admin client construction with prod-ref guard at construction time
    - Playwright setup project + storageState pattern for share-once auth
    - Path A test-data discipline (no destructive helpers in fixtures)
key_files:
  created:
    - e2e/fixtures.ts
    - e2e/auth.setup.ts
    - e2e/dashboard-smoke.spec.ts
  modified:
    - .env.example
decisions:
  - Adapted Sandra CRM fixtures structure but dropped CRM-specific helpers (resetTenantTables, seedProspects, seedList) that have no BMH analog
  - Used untyped SupabaseClient instead of generic Database type because src/lib/supabase/types.ts does not exist in BMH (TYPE-01 territory in Phase 4)
  - Prod-ref guard refuses construction when TEST_SUPABASE_URL contains dhvfsyteqsxagokoerrx (BMH prod project, the only Supabase project this repo has)
  - Path A scope locked: fixtures expose only adminClient + ensureTestUser + prod-ref guard; write-path helpers for HARDEN-02/HARDEN-03 deferred until Path B or Path C lock
metrics:
  duration: ~12 minutes
  tasks_completed: 3
  files_changed: 4
  commits: 2
  completed: 2026-05-01
---

# Phase 01.1 Plan 2: Playwright e2e harness Summary

One-liner: Local-dev Playwright e2e harness installed (e2e/fixtures.ts, e2e/auth.setup.ts, dashboard smoke spec, .env.example documentation), with a construction-time prod-ref guard that refuses to instantiate the service-role admin client against the BMH Institute prod Supabase project.

## Outcome

TPAR-02 closed. The local-dev e2e harness is wired end-to-end:

- `e2e/fixtures.ts` exposes `adminClient()`, `ensureTestUser()`, `TEST_USER_EMAIL`, `TEST_USER_PASSWORD`. Three references to `dhvfsyteqsxagokoerrx` (one in the comment that explains the guard, one in the conditional, one in the error message) and zero references to the Sandra CRM prod ref `copflsklaefwzipsrjqz`. No `import type { Database }` (the file does not exist in this repo).
- `e2e/auth.setup.ts` runs as a Playwright `setup` project, calls `ensureTestUser` via the admin client, signs in through the BMH `/login` form using the same selectors as `e2e-prod/auth.setup.ts`, waits for the `/dashboard` or `/auth/set-password` redirect, and writes storage state to exactly `e2e/.auth/user.json` (matching `playwright.config.ts` line 82 `storageState`).
- `e2e/dashboard-smoke.spec.ts` navigates to `/dashboard` (relative path so the config baseURL resolves to `localhost:3200`), asserts the URL settles, and checks the `Your training` heading is visible. Mirrors the shape of `e2e-prod/dashboard.spec.ts`.
- `.env.example` documents the three `TEST_SUPABASE_*` keys with the Path A intent recorded inline. Existing lines untouched.

`npx playwright test --list` resolves both projects:

```
[setup] auth.setup.ts:16:6  authenticate
[chromium] dashboard-smoke.spec.ts:7:5  dashboard renders for the authenticated test user
Total: 2 tests in 2 files
```

## Commits

Two commits in TDD order on branch `worktree-agent-a53a5d466ccc7c89d`:

| Order | Hash | Message |
|-------|------|---------|
| 1 | 00ec0c6 | test(01.1-2): TPAR-02 add failing e2e dashboard smoke spec |
| 2 | b0e9dc1 | feat(01.1-2): TPAR-02 install local-dev e2e harness with prod-ref guard |

Files in commit 1: `e2e/dashboard-smoke.spec.ts` (new, 13 lines).
Files in commit 2: `e2e/fixtures.ts` (new, 57 lines), `e2e/auth.setup.ts` (new, 28 lines), `.env.example` (modified, +12 lines append-only).

`playwright.config.ts` and `playwright.prod.config.ts` are unchanged from current `main` HEAD.

## TDD Gate Compliance

- RED gate: commit `00ec0c6` (`test(01.1-2): TPAR-02 add failing e2e dashboard smoke spec`) lands the spec in isolation. Running `npm run test:e2e` at this commit fails: webServer cannot boot because `TEST_SUPABASE_*` env vars are missing in `.env.test.local` (Playwright's webServer hook crashes Next dev when middleware tries to construct a Supabase client without keys). Even if the webServer did boot, the chromium project depends on the `setup` project, and `e2e/auth.setup.ts` does not yet exist. Both failure shapes are documented in the commit body.
- GREEN gate: commit `b0e9dc1` (`feat(01.1-2): TPAR-02 install local-dev e2e harness with prod-ref guard`) installs `e2e/fixtures.ts` and `e2e/auth.setup.ts`, plus the `.env.example` documentation. `npx playwright test --list` resolves both setup and chromium projects (output shown above). The smoke spec will pass on a real run once a contributor populates `.env.test.local` with `TEST_SUPABASE_*` env vars pointing at a non-prod Supabase project.
- REFACTOR gate: not exercised; no refactor needed for a 98-line three-file harness.

The TDD inventory was reviewed and locked in `.planning/phases/01.1-testing-coverage-parity/01.1-PLAN-CHECK.md` (PASS WITH CONCERNS, criterion 2 "Plan 2: T2 + T3 DELIVERED"). Plan 2 contained one user-visible e2e test plus the implicit `setup` project step. Both commits were authored against that locked inventory.

## Manual prod-ref-guard verification

The plan calls for a manual check that the prod-ref guard fires when `TEST_SUPABASE_URL` points at the BMH Institute prod project. Performed via a Node 25 strip-types script that imported `e2e/fixtures.ts` directly, set `TEST_SUPABASE_URL=https://dhvfsyteqsxagokoerrx.supabase.co` and a fake service-role key, then called `adminClient()`. Result:

```
PASS prod-ref guard threw with expected message:
  E2E fixtures refusing to run against the prod project (dhvfsyteqsxagokoerrx). Point TEST_SUPABASE_URL at a non-prod project.
PASS missing-env error threw with expected message:
  E2E fixtures need TEST_SUPABASE_URL + TEST_SUPABASE_SERVICE_ROLE_KEY in the environment.
```

The throw fires at `adminClient()` construction time, before any HTTP call. This satisfies threat T-01.1-2-01 (the only `mitigate` disposition in the plan's threat register).

No test was committed that exercises the guard against the live prod URL, by design: a live test would defeat the guard's purpose and risk a prod call if the conditional were ever reordered.

## Live e2e run note

`npm run test:e2e` was not executed against a real Supabase project because no non-prod BMH Institute Supabase project exists (project memory: `[No test Supabase project]`). The plan explicitly accepts this: "If `npm run test:e2e` fails because TEST_SUPABASE_* env vars are missing in `.env.test.local`, that is also acceptable; the contract for TPAR-02 is that the suite passes WHEN env vars are populated."

The harness wiring is verified by:
1. `npx playwright test --list` resolving both projects and finding both tests.
2. The manual prod-ref-guard check above.
3. `npm run typecheck` passing on the new TypeScript files.
4. File presence and content checks for every acceptance criterion.

A reviewer with access to a non-prod Supabase project can populate `.env.test.local` and confirm the smoke spec passes; this is logged as a follow-up in the deferred-items list rather than a blocker, per the plan-check MEDIUM-5 finding.

## Storage-state path confirmation

`e2e/.auth/user.json` is gitignored at `.gitignore` line 51 (`/e2e/.auth/`). The auth setup writes to that exact path; the chromium project reads from that exact path via `playwright.config.ts` line 82 `storageState: "e2e/.auth/user.json"`. No storage-state artifacts were committed in either commit.

## Files

| Path | Change | Lines |
|------|--------|-------|
| `e2e/fixtures.ts` | created | 57 |
| `e2e/auth.setup.ts` | created | 28 |
| `e2e/dashboard-smoke.spec.ts` | created | 13 |
| `.env.example` | modified (append-only) | +12 |

## Deviations from Plan

None of substance. Two minor textual deviations applied to honour the BMH writing-style rule "no em dashes" (CLAUDE.md, AGENTS.md, also flagged in PLAN-CHECK MEDIUM-1):

- The plan's `<interfaces>` block at lines 116-118 of the PLAN ships `— would trash data.` as the prod-ref guard error message. The committed `e2e/fixtures.ts` reads `... (dhvfsyteqsxagokoerrx). Point TEST_SUPABASE_URL at a non-prod project.` instead, swapping the em dash for a sentence break. Functionally identical: the error message still flags the project ref, and the project ref is still grep-checkable in the `acceptance_criteria` (which only requires `>= 1` reference to `dhvfsyteqsxagokoerrx`, not a specific message string).
- The plan's `.env.example` excerpt at lines 396-407 contains an em dash in `--- Local Vitest integration suite + npm run test:e2e ---`. The committed text drops the em dash from that header line and from inline prose. The block is otherwise verbatim.

These align the source-bound prose with project writing-style rules. No behavioural change.

No Rule 1, 2, 3, or 4 deviations triggered. No auth gates encountered.

## Authentication Gates

None. The harness is the auth gate; no external services were exercised.

## Self-Check: PASSED

File presence:
- `e2e/fixtures.ts`: FOUND
- `e2e/auth.setup.ts`: FOUND
- `e2e/dashboard-smoke.spec.ts`: FOUND
- `.env.example` (modified, appended): FOUND with new TEST_SUPABASE_* block

Commits exist in branch `worktree-agent-a53a5d466ccc7c89d`:
- `00ec0c6` (test commit): FOUND
- `b0e9dc1` (feat commit): FOUND

Acceptance criteria from PLAN.md `<acceptance_criteria>` blocks for Tasks 1, 2, 3 all pass. Phase-level `<verification>` checks all pass.

## Threat Flags

None. Every file in this plan was scoped in the plan's `<threat_model>` register; the prod-ref guard mitigates T-01.1-2-01, and T-01.1-2-02 / T-01.1-2-03 / T-01.1-2-04 retain their planned dispositions (accept / accept / mitigate via the grep-zero acceptance criterion).

## Known Stubs

None.

## Deferred Issues

None blocking. One follow-up captured for the test environment lock:

- Live `npm run test:e2e` run against a non-prod Supabase project remains unverified end-to-end. This is by Path A design (no test Supabase project exists). Resolution path: Jarrad locks Path B (Supabase ephemeral branches) or Path C (prod-with-prefix-cleanup), at which point a contributor populates `.env.test.local` and the smoke spec runs green on a real fixture project. Plan 3 of this phase records the deferral in `01-HUMAN-UAT.md`.
