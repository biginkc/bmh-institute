---
status: complete
phase: 01.1-testing-coverage-parity
source:
  - 01.1-1-rtl-test-infrastructure-SUMMARY.md
  - 01.1-2-SUMMARY.md
  - 01.1-3-SUMMARY.md
started: 2026-05-01T06:10:53Z
updated: 2026-05-01T06:14:00Z
---

## Current Test

[testing complete]

## Tests

### 1. RTL harness boots
expected: `npm run test:rtl` exits 0 with the PrintButton smoke spec reporting 1 passed, 0 failed.
result: pass

### 2. Verify chain runs end-to-end
expected: `npm run verify` exits 0. Output shows typecheck pass, unit suite green (~117 tests), RTL suite green (1 test).
result: pass

### 3. Playwright project resolution
expected: `npx playwright test --list` resolves both `[setup] auth.setup.ts` and `[chromium] dashboard-smoke.spec.ts`. Lists "Total: 2 tests in 2 files".
result: pass

### 4. Prod-ref guard fires
expected: Instantiating `adminClient()` from `e2e/fixtures.ts` with `TEST_SUPABASE_URL` pointing at the BMH prod ref `dhvfsyteqsxagokoerrx` throws an error refusing construction. Throw fires before any HTTP call.
result: pass

### 5. Integration tests skip cleanly
expected: `npm run test:integration` exits 0 when `TEST_SUPABASE_*` env is unset. Both `actions.integration.test.ts` and `answer-options-isolation.integration.test.ts` report as skipped, not failed or crashed.
result: pass

### 6. Prod e2e regression suite passes
expected: `npm run test:prod` exits 0. The new `admin-route-guard-learner.spec.ts` reports 4 cases passing (unauthenticated redirects on `/admin/reports/*`). Total run includes 12 cases (4 new + 8 existing) all green.
result: pass

### 7. Test environment runbook exists
expected: `docs/test-environment-setup.md` exists. Documents the three `TEST_SUPABASE_*` keys and the `E2E_*` keys. Lists command sequences for each test suite. Notes Path A as the current default.
result: pass

### 8. HUMAN-UAT closed-with-deferrals
expected: `.planning/phases/01-auth-and-access-hardening/01-HUMAN-UAT.md` frontmatter shows `status: closed-with-deferrals`. Every test item has a recorded `result:` line. Summary block totals reconcile to 5 (passed=3, deferred=1, closed=1).
result: pass

## Summary

total: 8
passed: 8
issues: 0
pending: 0
skipped: 0

## Gaps

[none yet]
